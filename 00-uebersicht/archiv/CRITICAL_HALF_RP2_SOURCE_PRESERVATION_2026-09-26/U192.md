# U192 — historische Originalfassung

**NOT_CANONICAL / HISTORICAL_SOURCE / NO_MATHEMATICAL_PROMOTION**

Diese Datei erhält die Originalbytes eines historischen Stands. Damalige Aussagen, Statuswörter und Prüfberichte werden als Quelle dokumentiert; sie erhalten hierdurch keine neue Geltung. Die fachliche Begutachtung wird in diesem Erhaltungspass nicht durchgeführt. Historischer Code, Workflow- und Statusdateien sind ausschließlich als Text eingebettet und werden nicht aktiviert.

- Versions-ID: `U192`.
- Quellcommit: `04e2256919d2726c31db7febe7810a137a8ff77d`.
- Originalpfad: `audits/P11_PRIME_CIRCLE_WEYL_AND_COMMON_SELF_ENERGY_2026-09-15.md`.
- Originalblob: `c82a293547446d4506ed705ebd54d9f719f0b080`.
- SHA-256 der Originalbytes: `9d3e79661250e95203718128ab7aeff8becf34d9f26b4effe95c85edde688741`.
- Umfang: 17236 Bytes; UTF-8, LF; abschließendes LF im Original: ja.
- Zusätzliche Trennbytes vor dem Schluss-Fence: 0. Diese gehören nicht zum Original; maßgeblich sind die ersten 17236 Bytes nach dem Eröffnungs-Fence.
- [Original im Commit](https://github.com/Waschtl904/objekt-x-programm/blob/04e2256919d2726c31db7febe7810a137a8ff77d/audits/P11_PRIME_CIRCLE_WEYL_AND_COMMON_SELF_ENERGY_2026-09-15.md) · [Familienindex](README.md).

Der folgende Textblock enthält unveränderte Originalbytes. Relative Quellverweise behalten ihren damaligen Pfad-/Commitkontext; sie werden hier nicht als aktive Links oder Abhängigkeiten ausgeführt.

``````````text
# P11 Audit — Prime circles, Weyl self-energies, and the common Critical-half bulk pattern

**Datum:** 15. September 2026  
**Basis:** Critical-half Green/tree, Prime-AR(1)/Poisson bridge, RP2 kernel/Line-Weyl audits, eingefroren bei `620695070db6ce9693ed5a1848a2fa66145f41ec`.  
**Rolle:** theorem-level geometrische Identifikation des vollen Prime-Power-Turms und gemeinsame Prime/Archimedes-Weyl-Normalform.  
**Registry:** unveraendert.  
**Nonclaim:** kein positiver Gesamtblock, kein all-window NP-GAP, kein Object-X-Abschluss, kein RH-Beweis, keine Publikationsneuheit.

---

## 0. Kurzurteil

Fuer jede Primzahl `p` besitzt der vollstaendige Prime-Power-Turm eine exakte kompakte Green-/Weyl-Geometrie.

Setze

```math
\ell_p:=\log p,
\qquad
q_p:=e^{-\ell_p/2}=p^{-1/2}.
```

Betrachte den Kreis

```math
C_p:=\mathbb R/\ell_p\mathbb Z
```

und darauf den Critical-half-Operator

```math
L_{p,\theta}
=-\partial_s^2+\frac14
```

mit quasi-periodischer Randbedingung/Holonomie

```math
u(s+\ell_p)=e^{i\theta}u(s),
\qquad
u'(s+\ell_p)=e^{i\theta}u'(s).
```

Dann ist der diagonale Green-Wert am Basispunkt exakt

```math
\boxed{
G_{p,\theta}(0,0)
=P_{q_p}^{\mathbb D}(\theta)
=
\frac{1-q_p^2}{1-2q_p\cos\theta+q_p^2}.
}
```

Die Method-of-Images-Reihe lautet

```math
\boxed{
G_{p,\theta}(0,0)
=
\sum_{k\in\mathbb Z}
q_p^{|k|}e^{ik\theta}.
}
```

Der Term `k` ist die `k`-fache Windung um den Prime-Kreis. Die Prime-Power-Gewichte `p^{-k/2}` sind daher exakt die Critical-half-Green-Amplituden der Windungssektoren.

Damit entsteht ein Covering-Diagramm:

```text
Critical-half Green on R
        |
        | sample at j log p, k log p
        v
P11 same-prime AR(1): p^{-|j-k|/2}
        |
        | quotient R -> R/(log p)Z + holonomy
        v
full prime tower: P_{p^-1/2}^D(z log p)
```

P11-AR(1) und der volle Weil-Prime-Turm sind somit **Universalcover- und Quotientenmanifestationen desselben Critical-half-Operators**.

Noch staerker: Nach einer exakt kontrollierten Gauge-Vervollstaendigung besitzt jeder Prime-Kanal dieselbe Weyl-Differenz-Struktur wie der RP2/Gamma-Kanal:

```text
positive uncentered energy = zero-frequency self-energy - current self-energy,
diagonal threshold         = zero-frequency self-energy,
centered contribution      = minus current self-energy.
```

Status:

```text
prime circle twisted Green diagonal = disk Poisson          ✓[M]
prime powers = winding sectors                              ✓[M]
P11 same-prime AR1 = universal-cover sampling               ✓[M]
prime tower = holonomy quotient of same Green kernel         ✓[M]
prime gauge-completed positive Weyl difference              ✓[M]
prime threshold = zero-frequency winding self-energy        ✓[M]
prime centered channel = minus winding self-energy          ✓[M]
Gamma channel has identical Weyl-difference architecture    ✓[M]
common longitudinal boundary type                           ✓[M]
rigorous common boundary-triple/Feshbach gluing             ?[O]
centered Weil positivity / Object X                         ?[O]
publication novelty                                         ?[O]
```

---

# 1. Universalcover: der Critical-half-Green-Kern

Fuer

```math
L_{1/2}
=-\partial_s^2+\frac14
```

auf der reellen Linie ist der Green-Kern

```math
\boxed{
g(s,s')=e^{-|s-s'|/2}.}
```

Der allgemeine massive Linienkern ist

```math
\frac1{2\mu}e^{-\mu|s-s'|};
```

bei `mu=1/2` ist der Vorfaktor exakt `1`.

Fixiere eine Primzahl `p` und die Gitterpunkte

```math
s_j=j\ell_p,
\qquad
s_k=k\ell_p.
```

Dann

```math
\begin{aligned}
g(s_j,s_k)
&=e^{-|j-k|\ell_p/2}\\
&=p^{-|j-k|/2}\\
&=q_p^{|j-k|}.
\end{aligned}
```

Somit

```math
\boxed{
G_{P11}^{(p)}(j,k)
=g(j\log p,k\log p).
}
```

Der P11-AR(1)-Block ist exakt die Gitterabtastung des Critical-half-Linien-Greenkerns.

Dies war projektintern bereits bewiesen; hier wird es als Universalcover-Seite der Kreisgeometrie fixiert.

---

# 2. Quotient zum Prime-Kreis

Setze

```math
C_p=\mathbb R/\ell_p\mathbb Z.
```

Fuer `theta in R` betrachte den flachen komplexen Linienbuendelkanal mit Holonomie

```math
e^{i\theta}.
```

Aequivalent arbeitet man auf Funktionen `u` auf `R` mit

```math
u(s+\ell_p)=e^{i\theta}u(s).
```

Der Green-Kern auf dem Quotienten wird per Method of Images aus dem Linienkern aufgebaut:

```math
\boxed{
G_{p,\theta}(s,s')
=
\sum_{n\in\mathbb Z}
e^{-in\theta}
 e^{-|s-s'+n\ell_p|/2}.
}
```

Die Reihe konvergiert absolut und lokal gleichmaessig, weil

```math
q_p=e^{-\ell_p/2}<1.
```

Am Basispunkt `s=s'=0` folgt

```math
\begin{aligned}
G_{p,\theta}(0,0)
&=
\sum_{n\in\mathbb Z}
q_p^{|n|}e^{-in\theta}\\
&=1+2\sum_{n\ge1}q_p^n\cos(n\theta).
\end{aligned}
```

Die geometrische Reihe ergibt

```math
\boxed{
G_{p,\theta}(0,0)
=
\frac{1-q_p^2}
{1-2q_p\cos\theta+q_p^2}
=P_{q_p}^{\mathbb D}(\theta).
}
```

Da der Ausdruck reell und gerade in `theta` ist, ist die Vorzeichenkonvention der Holonomie fuer die diagonale Aussage irrelevant.

### Klassische Firewall

Quasi-periodische Green-Funktionen und Method-of-Images/Lattice-Sum-Darstellungen sind klassische Analysis. Diese allgemeine Technik wird nicht als Neuheit beansprucht.

---

# 3. Hyperbolische Normalform

Mit

```math
q_p=e^{-\ell_p/2}
```

folgt durch Multiplikation von Zaehler und Nenner mit `e^{\ell_p/2}`

```math
\boxed{
P_{q_p}^{\mathbb D}(\theta)
=
\frac{\sinh(\ell_p/2)}
{\cosh(\ell_p/2)-\cos\theta}.
}
```

Dies ist die Standardform der massiven Kreis-Greenfunktion mit Twist/Holonomie.

Fuer `theta=0`:

```math
P_{q_p}^{\mathbb D}(0)
=
\frac{1+q_p}{1-q_p}.
```

---

# 4. Prime Powers sind Windungen

Die Method-of-Images-Formel zerlegt die Quotienten-Greenfunktion nach Decktransformationen

```math
n\ell_p,
\qquad n\in\mathbb Z.
```

Die nichttrivialen Windungsbeitraege sind

```math
q_p^{|n|}e^{in\theta}.
```

Fuer `n=k>=1` gilt

```math
q_p^k=p^{-k/2}.
```

Damit ist die projektweite Prime-Power-Amplitude exakt die Propagationsamplitude einer `k`-fachen Windung um den Kreis `C_p`.

Unter

```math
\theta=z\ell_p=z\log p
```

liefert das Paar der Windungen `+k,-k`

```math
2p^{-k/2}\cos(kz\log p),
```

genau den Fourierterm des kompletten zentrierten Prime-Turms.

Also:

```math
\boxed{
\text{prime power }p^k
\longleftrightarrow
\text{winding number }k
\text{ on }C_p.
}
```

Dies ist eine exakte projektinterne Identifikation, keine metaphorische Zuordnung.

---

# 5. Prime-Weyl-Self-energy

Definiere die nichttriviale Windungs-Self-energy

```math
\boxed{
W_p(\theta)
:=G_{p,\theta}(0,0)-1
=P_{q_p}^{\mathbb D}(\theta)-1.
}
```

Der abgezogene `1`-Term ist exakt die `n=0`-Decktransformation, also der direkte Universalcover-Greenwert am Ursprung.

Explizit

```math
\boxed{
W_p(\theta)
=2\sum_{k\ge1}q_p^k\cos(k\theta).
}
```

und

```math
\boxed{
W_p(0)=2\sum_{k\ge1}q_p^k
=\frac{2q_p}{1-q_p}.
}
```

---

# 6. Gauge-vervollstaendigte positive Prime-Energie

Sei `v` in `[-a,a]` getragen und `p<=e^{2a}`. Definiere den vollstaendigen positiven Prime-Turm

```math
\boxed{
\widetilde{\mathcal E}_{p,a}(v)
:=
\sum_{k\ge1}
(\log p)q_p^k
\|K_{k\ell_p}v\|_2^2.
}
```

Die Reihe konvergiert absolut, da

```math
\|K_t\|\le2
```

und `sum q_p^k<infty`.

Unter der unitaeren Fouriertransformation besitzt `K_t^*K_t` das Symbol

```math
2-2\cos(zt).
```

Daher ist das Symbol des kompletten positiven `p`-Turms

```math
\begin{aligned}
&2\log p
\sum_{k\ge1}q_p^k
[1-\cos(k\theta)]\\
&\qquad
=\log p[W_p(0)-W_p(\theta)],
\end{aligned}
```

mit `theta=z log p`.

Somit

```math
\boxed{
\widetilde{\mathcal E}_{p,a}
\longleftrightarrow
(\log p)[W_p(0)-W_p(z\log p)].
}
```

Die Positivitaet ist zugleich aus der Jump-Norm und aus der Tatsache sichtbar, dass der Poisson-Kern sein Maximum bei `theta=0` besitzt.

---

# 7. Vervollstaendigte Diagonalmasse und Zentrierung

Die Diagonalmasse des vollstaendigen Prime-Turms ist

```math
\begin{aligned}
\widetilde\Gamma_p
&=2\sum_{k\ge1}(\log p)q_p^k\\
&=(\log p)W_p(0).
\end{aligned}
```

Daher

```math
\begin{aligned}
\widetilde{\mathcal E}_{p,a}
-\widetilde\Gamma_p\|v\|^2
\end{aligned}
```

hat Symbol

```math
\boxed{
-(\log p)W_p(z\log p)
=(\log p)[1-P_{q_p}^{\mathbb D}(z\log p)].
}
```

Dies ist exakt der bereits bewiesene zentrierte Prime-Tower.

---

# 8. Warum diese Vervollstaendigung den urspruenglichen centered channel nicht aendert

Der originale finite-window COMMON-JUMP summiert nur Prime-Powers mit

```math
k\ell_p\le2a.
```

Fuer

```math
k\ell_p>2a
```

sind die beiden verschobenen Traeger von `v` disjunkt. Deshalb

```math
\boxed{
\|K_{k\ell_p}v\|^2
=2\|v\|^2.
}
```

Jeder hinzugefuegte Exterior-Term erfuellt daher einzeln

```math
(\log p)q_p^k
[\|K_{k\ell_p}v\|^2-2\|v\|^2]
=0.
```

Also ist die unendliche Vervollstaendigung eine reine **Cutoff-Gauge-Vervollstaendigung**:

```math
\boxed{
Q_{p,a}^{centered}
=
\widetilde{\mathcal E}_{p,a}
-\widetilde\Gamma_p I
}
```

auf dem Fensterraum, obwohl positive Energie und Threshold einzeln gegenueber der urspruenglichen endlichen Buchung erweitert wurden.

### Firewall

Diese Vervollstaendigung darf nicht als Gleichheit der **ungezentrierten** endlichen P11/COMMON-JUMP-Energie mit dem unendlichen Turm missverstanden werden. Exakt invariant ist ihre Differenz nach der gleichzeitig erweiterten Diagonalbuchung.

---

# 9. Dasselbe Muster auf der Gamma-/RP2-Seite

Aus dem RP2-Line-Weyl-Audit existiert eine regularisierte Punkt-/Line-Weyl-Funktion

```math
\Sigma(z)
```

mit

```math
\boxed{
\Phi_\infty(z)
=2\pi[\Sigma(0)-\Sigma(z)],
}
```

```math
\boxed{
\kappa_*=2\pi\Sigma(0),
}
```

und daher

```math
\boxed{
\Phi_\infty(z)-\kappa_*
=-2\pi\Sigma(z).
}
```

Damit stimmen Prime- und Gamma-Struktur formal exakt ueberein:

| Kanal | Self-energy | positive Energie | Threshold | centered |
|---|---|---|---|---|
| Prime `p` | `W_p(theta)` | `log p [W_p(0)-W_p(theta)]` | `log p W_p(0)` | `-log p W_p(theta)` |
| Gamma | `Sigma(z)` | `2pi [Sigma(0)-Sigma(z)]` | `2pi Sigma(0)` | `-2pi Sigma(z)` |

Dies ist keine numerische Analogie, sondern eine exakte algebraische Identitaet der beiden Kanalarchitekturen.

---

# 10. Gemeinsame Weyl-Self-energy des fixed-window-Problems

Fuer ein festes Fenster `a` tragen im zentrierten Prime-Teil nur Primzahlen

```math
p\le e^{2a}
```

nichttrivial bei. Definiere

```math
\boxed{
\Omega_a(z)
:=
2\pi\Sigma(z)
+
\sum_{p\le e^{2a}}
(\log p)W_p(z\log p).
}
```

Dann ist die gesamte zentrierte Symbolfunktion der harmonischen COMMON-JUMP-/Weil-Normalform

```math
\boxed{-\Omega_a(z).}
```

und auf NULLPOL

```math
\boxed{
Q_W(v)
=-\int_{\mathbb R}
|\widehat v(z)|^2\Omega_a(z)\,dz.
}
```

mit der projektweit verwendeten unitaeren Fouriernormalisierung.

Die entsprechende gauge-vervollstaendigte positive Backbone besitzt das Symbol

```math
\boxed{
\mathcal E_a(z)
=
2\pi[\Sigma(0)-\Sigma(z)]
+
\sum_{p\le e^{2a}}
(\log p)
[W_p(0)-W_p(z\log p)].
}
```

und die vervollstaendigte Diagonalmasse ist

```math
\boxed{
\widetilde\Gamma_a
=
2\pi\Sigma(0)
+
\sum_{p\le e^{2a}}
(\log p)W_p(0).
}
```

Daher exakt

```math
\boxed{
\mathcal E_a(z)-\widetilde\Gamma_a
=-\Omega_a(z).
}
```

---

# 11. Longitudinaler Bulk fuer einen Prime-Kreis

Die Holonomie kann als longitudinale Fouriervariable realisiert werden.

Betrachte auf

```math
L^2(\mathbb R_x\times C_p)
```

den positiven Operator

```math
\boxed{
\mathcal L_p
=(D_x+D_s)^2+\frac14,
}
```

wobei

```math
D_x=-i\partial_x,
\qquad
D_s=-i\partial_s
```

und `s` periodisch modulo `ell_p` ist.

Nach Fouriertransformation in `x` wird

```math
D_x\mapsto z,
```

also

```math
\mathcal L_p(z)
=(D_s+z)^2+\frac14.
```

Durch die Eichtransformation

```math
u(s)=e^{-izs}u(s)
```

wird der Differentialausdruck zu

```math
-\partial_s^2+\frac14,
```

waehrend die periodische Randbedingung in die Holonomie

```math
e^{\pm iz\ell_p}
```

uebergeht.

Daher ist die diagonale Line-Defect-Resolvente an

```math
\mathbb R_x\times\{s=0\}
```

im longitudinalen Fourierbild exakt

```math
\boxed{
G_p(z):=P_{q_p}^{\mathbb D}(z\ell_p).
}
```

Nach Abzug des Universalcover-Direktterms `1` ist

```math
W_p(z\ell_p)=G_p(z)-1.
```

Der Prime-Self-energy-Kanal ist damit ebenso wie der RP2/Gamma-Kanal ein **Line-defect Boundary-Response-Operator auf `L^2(R_x)`**.

---

# 12. Prime und Archimedes leben nun auf demselben Boundary-Typ

Wir haben zwei positive Bulk-Familien:

### Prime `p`

```math
\mathbb R_x\times S^1_{\log p},
\qquad
\mathcal L_p=(D_x+D_s)^2+1/4,
```

mit Line-Defect

```math
\mathbb R_x\times\{0\}.
```

### Archimedes

```math
\mathbb R_x\times RP^2,
\qquad
\mathcal L_\infty=D_x^2+\Delta_{RP^2}+1/4,
```

mit Line-Defect

```math
\mathbb R_x\times\{y_0\}.
```

Nach Eliminierung der transversalen Bulkvariablen entstehen jeweils skalare Boundary-Self-energies auf

```math
\boxed{\mathcal B=L^2(\mathbb R_x).}
```

Prime und Archimedes sind damit nicht nur harmonisch verwandt, sondern **Boundary-Operatoren desselben Typs**.

---

# 13. Gemeinsamer Critical-half-Shift

Sowohl Prime-Zylinder als auch RP2-Bulk verwenden denselben Shift

```math
\boxed{+1/4.}
```

Auf dem Prime-Kreis ist dies die Masse des Critical-half-Linienoperators

```math
-\partial_s^2+1/4.
```

Auf `RP^2` ist

```math
\Delta_{RP^2}+1/4=P^2.
```

Der gemeinsame Mittelpunkt `1/2` ist daher in beiden Bulkmodellen derselbe geometrische Massenshift, nicht lediglich ein Koeffizientenmatch.

---

# 14. Covering-Lesart des P11-Prime-Astes

Primeweise existiert nun die exakte Kette

```text
R universal cover
  |
  | sample at multiples of log p
  v
P11 AR(1) covariance q^{|j-k|}
  |
  | quotient by (log p)Z + holonomy e^{i theta}
  v
circle diagonal Green = P_q(theta)
  |
  | remove zero winding
  v
prime self-energy W_p(theta)
  |
  | zero-holonomy difference
  v
positive complete prime jump energy.
```

Dies erklaert gleichzeitig:

```text
q_p=p^-1/2,
prime powers k,
AR(1) |j-k|,
Poisson kernel,
and full p-power completion.
```

### Firewall

Der gesamte P11-Sternbaum ist damit noch nicht als Universalcover eines fertigen Bouquet-Operators identifiziert. Primeweise ist die Covering-Geometrie exakt; die cross-prime Rootverklebung, source conditioning und Feshbach-Struktur muessen separat respektiert werden.

---

# 15. Was ein gemeinsamer Boundary-Feshbach-Schritt nun leisten muesste

Die Gesamt-Self-energy

```math
\Omega_a(D_x)
```

ist eine Summe von Boundary-Antworten positiver Critical-half-Bulks.

Dies legt einen gemeinsamen Boundary-triple/Feshbach-Ansatz nahe: die Line-Defekte aller Prime-Zylinder und des `RP^2`-Bulks werden an dieselbe longitudinale Boundary-Variable gekoppelt.

Aber der Schritt ist **noch nicht bewiesen**.

Insbesondere muss eine zulaessige Konstruktion gleichzeitig:

```text
1. die P11 source-conditioned Hub/Rest-Feshbach-Geometrie reproduzieren;
2. die Prime-circle winding self-energies mit den realen spatial Jumpfeatures typkorrekt verbinden;
3. die RP2 point/line-Weyl finite part reproduzieren;
4. die R<S connecting maps und Exterior-before-overlap-Gauge respektieren;
5. die Zentrierung/finite-part nicht durch vorausgesetzte NP-GAP-Positivitaet definieren.
```

Eine bloße direkte Summe der Bulkoperatoren ist noch kein Object X.

---

# 16. Literatur-/Neuheitsfirewall

### Klassisch

- Green-Funktionen des massiven Operators auf `R` und `S^1`;
- quasi-periodische/twisted Randbedingungen;
- Method of Images / Lattice sums;
- Poisson-Kern und AR(1)-Spektraldichte;
- Kreise als Quotienten ihrer Universalcover;
- Boundary/Weyl-/Self-energy-Technik allgemein.

Quasi-periodische Green-Funktionen und Lattice-Sum-Darstellungen sind klassische Literatur; z.B. A. Moroz, *Quasi-periodic Green's functions of the Helmholtz and Laplace equations* (2006).

### Neu in dieser Untersuchung exakt hergeleitet

- die konkrete Primekreislaenge

  ```math
  \ell_p=\log p;
  ```

- P11-AR(1) als Universalcover-Abtastung und der volle projektinterne Prime-Power-Turm als Holonomie-Quotient **desselben** Critical-half-Greenoperators;
- Prime Powers als Windungssektoren dieses Quotienten;
- die exakte Prime-Weyl-Differenz-Normalform;
- die gemeinsame Prime/Gamma-Self-energy-Tabelle;
- die gemeinsame Longitudinal-Boundary-Typisierung durch Prime-Zylinder und `RP^2`-Bulk.

### Publication novelty

```text
?[O]
```

Keine weltweite Neuheit wird aus einer Suchluecke abgeleitet.

---

# 17. Forschungsurteil

Der Prime-/Archimedes-Unterschied hat sich weiter reduziert.

Vorher:

```text
Prime: discrete AR(1)/Poisson channels
Gamma: continuous digamma/resolvent layer
```

Jetzt:

```text
Prime p:
  critical-half cylinder R x S1_{log p}
  -> line-defect self-energy W_p(D_x log p)

Archimedes:
  critical-half product R x RP2
  -> line-defect self-energy Sigma(D_x)
```

und beide besitzen exakt

```math
\boxed{
\text{positive energy}
=
\text{self-energy at }0
-
\text{self-energy at }D_x.
}
```

Nach Abzug der Diagonal-/zero-frequency self-energy bleibt jeweils der negative aktuelle Weyl-Kanal.

Der naechste echte Gate ist daher nicht mehr die Suche nach einer gemeinsamen Kanalform. Sie existiert.

Gesucht ist jetzt eine **vorwaerts definierte gemeinsame Boundary-Verklebung** der Prime-Zylinder, des `RP^2`-Bulks und der bereits existierenden P11 Source/Rest/Feshbach-Geometrie, deren globaler finite-part/Shorting-Schritt exakt die zentrierte Weilform liefert.
``````````

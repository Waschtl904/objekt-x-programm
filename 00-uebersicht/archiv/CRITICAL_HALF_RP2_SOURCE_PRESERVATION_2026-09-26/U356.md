# U356 — historische Originalfassung

**NOT_CANONICAL / HISTORICAL_SOURCE / NO_MATHEMATICAL_PROMOTION**

Diese Datei erhält die Originalbytes eines historischen Stands. Damalige Aussagen, Statuswörter und Prüfberichte werden als Quelle dokumentiert; sie erhalten hierdurch keine neue Geltung. Die fachliche Begutachtung wird in diesem Erhaltungspass nicht durchgeführt. Historischer Code, Workflow- und Statusdateien sind ausschließlich als Text eingebettet und werden nicht aktiviert.

- Versions-ID: `U356`.
- Quellcommit: `6615399c3474bfbf81206e2571494b5d323faa87`.
- Originalpfad: `audits/P11_SCATTERING_POINTWISE_POSITIVITY_NOGO_2026-09-13.md`.
- Originalblob: `b58afb6978c23e33cdf3e30ba596aca71fb54ceb`.
- SHA-256 der Originalbytes: `b70beda507c25b1533aca1fbd5dfd09e8440279c55dccb688be92f8a098e55a0`.
- Umfang: 5820 Bytes; UTF-8, LF; abschließendes LF im Original: ja.
- Zusätzliche Trennbytes vor dem Schluss-Fence: 0. Diese gehören nicht zum Original; maßgeblich sind die ersten 5820 Bytes nach dem Eröffnungs-Fence.
- [Original im Commit](https://github.com/Waschtl904/objekt-x-programm/blob/6615399c3474bfbf81206e2571494b5d323faa87/audits/P11_SCATTERING_POINTWISE_POSITIVITY_NOGO_2026-09-13.md) · [Familienindex](README.md).

Der folgende Textblock enthält unveränderte Originalbytes. Relative Quellverweise behalten ihren damaligen Pfad-/Commitkontext; sie werden hier nicht als aktive Links oder Abhängigkeiten ausgeführt.

``````````text
# P11 Audit — Pointwise scattering-positivity No-Go

**Datum:** 13. September 2026  
**Basis:** Euler/Gamma scattering-phase realization auf dem Critical-half-Branch.  
**Rolle:** theorem-level Architekturfilter.  
**Registry:** unveraendert.  
**Nonclaim:** kein NP-GAP-/RH-Beweis.

---

## 0. Kurzurteil

Der explizite Fenster-Streumultiplikator

```math
\tau_a(z):=-i\overline{S_a(z)}S_a'(z)
```

ist fuer **jedes** `a>0` auf der reellen Achse zwingend vorzeichenwechselnd:

```math
\boxed{\tau_a(0)<0,}
```

waehrend

```math
\boxed{\tau_a(z)\to+\infty\qquad(|z|\to\infty).}
```

Damit ist jede Object-X-/Dilationklasse ausgeschlossen, deren Positivitaetsmechanismus eine punktweise nichtnegative Wigner--Smith-Dichte

```math
\tau_a(z)\ge0
```

oder eine punktweise Faktorisierung

```math
\tau_a(z)=B_a(z)^*B_a(z)
```

auf der reellen Frequenzachse verlangen wuerde.

Die gesuchte Positivitaet kann daher nur durch die **nichtlokale Fenster-/Paley--Wiener-Struktur und die NULLPOL-Range-Bedingung** entstehen.

Status:

```text
sign change of finite-window scattering delay           ✓[M]
pointwise-positive multiplier factorization              ×[M]
pointwise-positive Wigner-Smith dilation class           ×[M]
need for support/Paley-Wiener/nullpole compression       ✓[M]
sharp compressed NP-GAP                                  ?[O]
RH / full Object X                                       ?[O]
```

---

# 1. Fenster-Streufaktor

Aus dem Scattering-Audit:

```math
S_a(z)
=S_\infty(z)\prod_{p\le e^{2a}}S_p(z),
```

mit

```math
S_p(z)=
\frac{1-p^{-1/2}e^{iz\log p}}
     {1-p^{-1/2}e^{-iz\log p}},
```

```math
S_\infty(z)=
\pi^{-iz}
\frac{\Gamma(1/4+iz/2)}
     {\Gamma(1/4-iz/2)}.
```

Fuer reelles `z` ist `|S_a(z)|=1`, und

```math
\tau_a(z)
=
\operatorname{Re}\psi\left(\frac14+\frac{iz}{2}\right)-\log\pi
+
\sum_{p\le e^{2a}}
(\log p)
\left[
1-P_{p^{-1/2}}^{\mathbb D}(z\log p)
\right].
```

---

# 2. Exakter negativer Wert bei `z=0`

Archimedisch:

```math
\tau_\infty(0)
=\psi(1/4)-\log\pi
=-\kappa_*<0.
```

Fuer einen Prime-Faktor setze `q=p^{-1/2}`. Dann

```math
P_q^{\mathbb D}(0)
=\frac{1+q}{1-q},
```

also

```math
1-P_q^{\mathbb D}(0)
=-\frac{2q}{1-q}
=-\frac{2}{\sqrt p-1}.
```

Daher exakt

```math
\boxed{
\tau_a(0)
=-\kappa_*
-2\sum_{p\le e^{2a}}
\frac{\log p}{\sqrt p-1}
<0.
}
```

Die Summe ist endlich. Falls `e^{2a}<2`, ist sie leer; auch dann bleibt `tau_a(0)=-kappa_*<0`.

---

# 3. Positive Hochfrequenz

Aus der Digamma-Asymptotik

```math
\operatorname{Re}\psi\left(\frac14+\frac{iz}{2}\right)-\log\pi
=
\log\frac{|z|}{2\pi}+O(|z|^{-2})
```

folgt archimedisch logarithmisches Wachstum gegen `+infty`.

Fuer jedes feste `p` ist dagegen

```math
(\log p)[1-P_{p^{-1/2}}^{\mathbb D}(z\log p)]
```

eine stetige beschraenkte periodische Funktion. Im Fensterprodukt treten nur endlich viele Primzahlen `p<=e^{2a}` auf. Daher existiert eine Konstante `C_a<infty` mit

```math
\left|
\sum_{p\le e^{2a}}
(\log p)[1-P_{p^{-1/2}}^{\mathbb D}(z\log p)]
\right|
\le C_a.
```

Somit

```math
\boxed{
\tau_a(z)
=
\log\frac{|z|}{2\pi}+O_a(1)
\longrightarrow+\infty.
}
```

Zusammen mit `tau_a(0)<0` und Stetigkeit folgt zwingend ein Vorzeichenwechsel.

---

# 4. No-Go fuer punktweise Gram-/Time-delay-Positivitaet

Angenommen, eine vorgeschlagene Object-X-Architektur wuerde auf der reellen Frequenzachse eine direkte punktweise Faktorisierung liefern

```math
\tau_a(z)=B_a(z)^*B_a(z).
```

Dann waere notwendigerweise

```math
\tau_a(z)\ge0
```

fuer alle reellen `z`, im Widerspruch zu §2.

Daher

```math
\boxed{
\text{direct pointwise multiplier Gram factorization}
\quad\times[M].
}
```

Dasselbe gilt fuer jede conservative scattering-Dilationklasse, fuer welche ein allgemeiner Satz die punktweise Positivitaet des Wigner--Smith-Operators auf dem vollen reellen Spektrum erzwingen wuerde.

### Firewall

Nicht ausgeschlossen sind positive **Kompressionen**, Shortings, Toeplitz-/Hankel-Formen oder reproduzierende-Kern-Konstruktionen auf einem echten Unterraum. Genau dort koennen die Fenster-/Paley--Wiener- und NULLPOL-Bedingungen wesentlich werden.

---

# 5. Warum dies mit NP-GAP vereinbar ist

Auf NULLPOL gilt in Fensterquadratikform

```math
Q_W(v)
=\int|\widehat v(z)|^2\tau_a(z)dz.
```

Die Funktionen `widehat v` sind jedoch keine beliebigen `L^2`-Gewichte:

1. `supp v subset [-a,a]` zwingt eine Paley--Wiener-Struktur vom Exponentialtyp `a`;
2. NULLPOL ist exakt
   ```math
   v=L_{1/2}u,
   ```
   also auf Fourierseite die feste Multiplikation mit `z^2+1/4` aus einem wiederum fenstergetragenen `u`;
3. aequivalent liegen die komplexen Nullbedingungen bei `z=±i/2` vor.

Daher widerspricht die punktweise Negativitaet von `tau_a` nicht einer moeglichen Positivitaet der **komprimierten Form**.

Gerade dies zeigt, dass NP-GAP wesentlich ein Unschaerfe-/Hardy-/Paley--Wiener-Problem ist und nicht durch eine punktweise Symboluntergrenze geloest werden kann.

---

# 6. Neuer Architekturfilter

Ab jetzt sind als Hauptkandidaten nur Mechanismen interessant, die die Kompression ernsthaft benutzen, zum Beispiel:

```text
Paley-Wiener Toeplitz/Hankel shorting,
Hardy/model-space compression,
de-Branges/reproducing-kernel geometry,
positive harmonic extension with nontrivial boundary Schur complement,
critical-half source lift plus Tree/Gamma coupling.
```

Nicht weiter verfolgen:

```text
pointwise tau_a >= 0,
direct multiplication Gram tau_a=B^*B,
"unitary scattering implies positive time delay",
channelwise lower bounds that ignore the window subspace.
```

---

# 7. Firewalls

Nicht behaupten:

- das No-Go widerlege NP-GAP;
- eine vorzeichenwechselnde Wigner--Smith-Dichte verhindere positive komprimierte Quadratikformen;
- Hardy-/de-Branges-Positivitaet sei bereits bewiesen;
- RH sei widerlegt oder bewiesen;
- Publikationsneuheit sei geklaert.
``````````

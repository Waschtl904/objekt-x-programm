# U362 — historische Originalfassung

**NOT_CANONICAL / HISTORICAL_SOURCE / NO_MATHEMATICAL_PROMOTION**

Diese Datei erhält die Originalbytes eines historischen Stands. Damalige Aussagen, Statuswörter und Prüfberichte werden als Quelle dokumentiert; sie erhalten hierdurch keine neue Geltung. Die fachliche Begutachtung wird in diesem Erhaltungspass nicht durchgeführt. Historischer Code, Workflow- und Statusdateien sind ausschließlich als Text eingebettet und werden nicht aktiviert.

- Versions-ID: `U362`.
- Quellcommit: `5b5f17a5afd45e8f7b15bc3121b8a869dceb28a1`.
- Originalpfad: `audits/P11_TRANSVERSE_VOLUME_TRACE_NORMALIZATION_2026-09-15.md`.
- Originalblob: `ff2bfd27397e3b2d9903f13a15ad0df14e0b7106`.
- SHA-256 der Originalbytes: `a903956ab4bd491ff96ac88c29e711f354c401a73d7285409450f4feacaa5182`.
- Umfang: 10435 Bytes; UTF-8, LF; abschließendes LF im Original: ja.
- Zusätzliche Trennbytes vor dem Schluss-Fence: 0. Diese gehören nicht zum Original; maßgeblich sind die ersten 10435 Bytes nach dem Eröffnungs-Fence.
- [Original im Commit](https://github.com/Waschtl904/objekt-x-programm/blob/5b5f17a5afd45e8f7b15bc3121b8a869dceb28a1/audits/P11_TRANSVERSE_VOLUME_TRACE_NORMALIZATION_2026-09-15.md) · [Familienindex](README.md).

Der folgende Textblock enthält unveränderte Originalbytes. Relative Quellverweise behalten ihren damaligen Pfad-/Commitkontext; sie werden hier nicht als aktive Links oder Abhängigkeiten ausgeführt.

``````````text
# P11 Audit — transverse volume normalization and local resolvent traces

**Datum:** 15. September 2026  
**Basis:** Prime-Circle-Weyl + RP2 Line-Weyl + Total Boundary-Weyl normal form.  
**Rolle:** theorem-level geometrische Herkunft der Faktoren `log p` und `2pi` sowie Trace-Normalisierung der lokalen Self-energies.  
**Registry:** unveraendert.  
**Nonclaim:** kein NP-GAP, kein Object-X-Abschluss, kein RH-Beweis, keine Publikationsneuheit.

---

## 0. Kurzurteil

Die natuerlichen Koeffizienten der totalen Boundary-Self-energy sind exakt die Volumina der zugehoerigen kompakten transversalen Raeume:

```math
\boxed{
\log p=\operatorname{vol}(S^1_{\log p}),
\qquad
2\pi=\operatorname{vol}(RP^2).
}
```

Dadurch werden die bisher punktweise formulierten Weyl-/Green-Diagonalen zu vollen transversalen Resolventenspuren.

Fuer den Prime-Zylinder sei

```math
A_p(z)
:=(D_s+z)^2+\frac14
```

auf dem Kreis `C_p=S^1_{ell_p}`, `ell_p=log p`. Dann

```math
\boxed{
\operatorname{Tr}_{C_p}A_p(z)^{-1}
=\ell_p P_{p^{-1/2}}^{\mathbb D}(z\ell_p).
}
```

Fuer den archimedischen Bulk sei

```math
A_\infty(z)
:=\Delta_{RP^2}+\frac14+z^2.
```

Dann

```math
\boxed{
\operatorname{FP}\operatorname{Tr}_{RP^2}A_\infty(z)^{-1}
=2\pi\Sigma_\infty(z).
}
```

Damit ist die gauge-vervollstaendigte positive Backbone exakt

```math
\boxed{
\begin{aligned}
\widetilde{\mathcal E}_a(z)
&=
\operatorname{Tr}_{RP^2}
[A_\infty(0)^{-1}-A_\infty(z)^{-1}]\\
&\quad+
\sum_{p\le e^{2a}}
\operatorname{Tr}_{C_p}
[A_p(0)^{-1}-A_p(z)^{-1}].
\end{aligned}
}
```

Die Prime-/Archimedes-Gewichte sind somit keine nachtraeglichen arithmetischen Vorfaktoren mehr, sondern geometrische Trace-Volumenfaktoren.

Status:

```text
prime log p = circle volume                              ✓[M]
2pi = round RP2 volume                                   ✓[M]
prime weighted Poisson = circle resolvent trace          ✓[M]
Gamma weighted Weyl = RP2 regularized resolvent trace    ✓[M]
positive backbone = sum of resolvent trace differences   ✓[M]
completed threshold = renormalized zero-frequency traces ✓[M]
universal upper-half-plane pole residue -i               ✓[M]
full local-space unification beyond trace level           ?[O]
Object X / NP-GAP / RH                                    ?[O]
```

---

# 1. Prime circle volume

For

```math
C_p=\mathbb R/\ell_p\mathbb Z,
\qquad
\ell_p=\log p,
```

with its standard flat measure,

```math
\boxed{\operatorname{vol}(C_p)=\ell_p=\log p.}
```

This is exactly the coefficient multiplying the full Prime-Power Poisson channel in the Weil multiplier.

---

# 2. Prime circle trace

On periodic `L^2(C_p)` define

```math
A_p(z)=(D_s+z)^2+1/4.
```

The circle is homogeneous (equivalently the flat connection has constant coefficients), so the diagonal of the resolvent kernel is constant in `s`.

The Prime-Circle-Weyl audit gives that constant diagonal:

```math
A_p(z)^{-1}(s,s)
=P_{q_p}^{\mathbb D}(z\ell_p),
\qquad
q_p=e^{-\ell_p/2}=p^{-1/2}.
```

Therefore

```math
\begin{aligned}
\operatorname{Tr}_{C_p}A_p(z)^{-1}
&=\int_{C_p}A_p(z)^{-1}(s,s)ds\\
&=\ell_p P_{q_p}^{\mathbb D}(z\ell_p).
\end{aligned}
```

Thus

```math
\boxed{
\operatorname{Tr}_{C_p}A_p(z)^{-1}
=(\log p)P_{p^{-1/2}}^{\mathbb D}(z\log p).
}
```

No external weight remains.

---

# 3. Prime self-energy as renormalized circle trace

Recall

```math
W_p(\theta)
=P_{q_p}^{\mathbb D}(\theta)-1.
```

Hence

```math
\boxed{
(\log p)W_p(z\log p)
=
\operatorname{Tr}_{C_p}A_p(z)^{-1}
-\operatorname{vol}(C_p).
}
```

The subtracted `vol(C_p)` is exactly the circle volume times the zero-winding universal-cover diagonal `1`.

At zero frequency:

```math
\boxed{
(\log p)W_p(0)
=
\operatorname{Tr}_{C_p}A_p(0)^{-1}
-\operatorname{vol}(C_p).
}
```

---

# 4. Positive Prime backbone as a trace difference

The completed positive Prime channel is

```math
(\log p)[W_p(0)-W_p(z\log p)].
```

The volume subtraction cancels, so

```math
\boxed{
(\log p)[W_p(0)-W_p(z\log p)]
=
\operatorname{Tr}_{C_p}
[A_p(0)^{-1}-A_p(z)^{-1}].
}
```

For real `z` this scalar trace difference is nonnegative because it equals the already positive completed COMMON-JUMP Prime energy symbol.

### Firewall

It is **not** asserted that the operator difference

```math
A_p(0)^{-1}-A_p(z)^{-1}
```

is positive on `L^2(C_p)` for every real `z`. The theorem-level positivity used here is the scalar trace identity / COMMON-JUMP symbol.

---

# 5. RP2 volume

For the unit round sphere

```math
\operatorname{vol}(S^2)=4\pi.
```

The antipodal quotient has half the volume:

```math
\boxed{
\operatorname{vol}(RP^2)=2\pi.
}
```

This is exactly the coefficient multiplying the regularized point-Weyl function in the Gamma layer.

---

# 6. RP2 regularized trace

Let

```math
A_\infty(z)
=\Delta_{RP^2}+1/4+z^2.
```

The Point-Weyl/Line-Weyl audits define the spectrally regularized diagonal

```math
\Sigma_\infty(z)
=\operatorname{FP}A_\infty(z)^{-1}(y,y),
```

independent of `y` by homogeneity.

Multiplication by the volume gives

```math
\boxed{
2\pi\Sigma_\infty(z)
=
\operatorname{FP}
\operatorname{Tr}_{RP^2}A_\infty(z)^{-1}.
}
```

This is exactly the archimedean self-energy appearing in `Omega_a`.

---

# 7. Positive Gamma backbone as an ordinary trace-class difference

Although the individual 2D resolvent trace needs a finite part, the difference

```math
A_\infty(0)^{-1}-A_\infty(z)^{-1}
```

is of order `-4` and trace class.

Hence no finite part is needed for the positive Gamma backbone:

```math
\boxed{
\Phi_\infty(z)
=
\operatorname{Tr}_{RP^2}
[A_\infty(0)^{-1}-A_\infty(z)^{-1}].
}
```

This is the exact archimedean counterpart of the Prime circle trace difference in §4.

---

# 8. Unified positive backbone

Adding all channels gives

```math
\boxed{
\begin{aligned}
\widetilde{\mathcal E}_a(z)
&=
\operatorname{Tr}_{RP^2}
[A_\infty(0)^{-1}-A_\infty(z)^{-1}]\\
&\quad+
\sum_{p\le e^{2a}}
\operatorname{Tr}_{C_p}
[A_p(0)^{-1}-A_p(z)^{-1}].
\end{aligned}
}
```

The total positive fixed-window/gauge-completed energy is therefore a sum of **transverse resolvent trace drops from zero longitudinal frequency**.

This formulation contains no explicit `log p` or `2pi` coefficients; they are already the transverse volumes inside the traces.

---

# 9. Unified zero-frequency diagonal mass

The completed Prime diagonal mass is

```math
\operatorname{Tr}_{C_p}A_p(0)^{-1}
-\operatorname{vol}(C_p).
```

The archimedean diagonal mass is

```math
\operatorname{FP}\operatorname{Tr}_{RP^2}A_\infty(0)^{-1}
=\kappa_*.
```

Thus the completed total threshold is

```math
\boxed{
\widetilde\Gamma_a
=
\operatorname{FP}\operatorname{Tr}_{RP^2}A_\infty(0)^{-1}
+
\sum_{p\le e^{2a}}
\left[
\operatorname{Tr}_{C_p}A_p(0)^{-1}
-\operatorname{vol}(C_p)
\right].
}
```

This is a purely spectral-geometric zero-frequency trace quantity.

---

# 10. Total centered self-energy as renormalized trace sum

Combining §§3 and 6:

```math
\boxed{
\Omega_a(z)
=
\operatorname{FP}\operatorname{Tr}_{RP^2}A_\infty(z)^{-1}
+
\sum_{p\le e^{2a}}
\left[
\operatorname{Tr}_{C_p}A_p(z)^{-1}
-\operatorname{vol}(C_p)
\right].
}
```

Thus the fixed-window Weil form is the negative compression of a **total renormalized transverse resolvent trace**.

This is the strongest geometric normalization currently available for `Omega_a`.

---

# 11. Universal critical-pole residues

The trace normalization also explains a residue rigidity.

## Prime

The Prime trace is

```math
\operatorname{Tr}_{C_p}A_p(z)^{-1}
=(\log p)
P_{q_p}^{\mathbb D}(z\log p).
```

Its meromorphic continuation has poles

```math
z=\frac{2\pi n}{\log p}\pm\frac i2.
```

At every upper pole

```math
\boxed{
\operatorname{Res}=-i.
}
```

The factor `log p` is exactly what cancels the derivative of the holonomy denominator and makes the residue independent of `p` and `n`.

## Gamma

The regularized RP2 trace has poles

```math
z=\pm i\mu_m,
\qquad
\mu_m=2m+1/2.
```

The `m`-th term of the volume-normalized trace is

```math
\frac{2\mu_m}{z^2+\mu_m^2}.
```

Therefore at every upper pole

```math
\boxed{
\operatorname{Res}_{z=i\mu_m}=-i.
}
```

The equality

```math
\dim E_m=2\mu_m
```

is exactly what makes the residue universal.

Hence **all naturally volume-normalized local self-energy poles have the same residue `-i` in the upper half-plane**.

---

# 12. Shared Critical-half pole

Every Prime channel has in particular the pole

```math
z=i/2
```

corresponding to `n=0`.

The Gamma channel has the same pole at its ground level

```math
\mu_0=1/2.
```

Thus the critical pole

```math
\boxed{z=i/2}
```

is common to **all** local channels with the same residue normalization.

The conjugate lower pole is at `-i/2` with residue `+i`.

The compact-window NULLPOL conditions are precisely

```math
\widehat v(i/2)=\widehat v(-i/2)=0.
```

This gives a new geometric reading of the Critical-half pole cancellation.

### Firewall

Prime circles also have side poles

```math
2\pi n/\log p\pm i/2,
\qquad n\ne0,
```

which NULLPOL does **not** cancel. Therefore this shared central-pole observation does not justify a naive contour-shift positivity argument; it is compatible with the existing Blaschke/periodic-pole no-go.

---

# 13. Literature/novelty firewall

Homogeneous resolvent diagonals, trace = volume times constant diagonal, twisted circle Green functions, RP2 spectra and residue calculations are classical consequences of standard spectral theory.

No novelty is claimed for those ingredients.

Project-specific new-here synthesis:

```text
log p = prime-circle volume,
2pi = RP2 volume,
Weil local weights = transverse trace normalization,
common positive backbone = sum of resolvent trace differences,
universal unit residue after natural volume normalization.
```

Publication novelty remains `?[O]`.

---

# 14. Forschungsurteil

The local data now admit a fully geometric table:

| channel | transverse space | volume | positive backbone | centered self-energy |
|---|---|---:|---|---|
| prime `p` | `S1_{log p}` | `log p` | `Tr[A_p(0)^-1-A_p(z)^-1]` | `-[Tr A_p(z)^-1-vol]` |
| Gamma | `RP2` | `2pi` | `Tr[A_inf(0)^-1-A_inf(z)^-1]` | `-FP Tr A_inf(z)^-1` |

This removes the last apparently external local coefficients from the common Weyl architecture.

What remains unexplained is not the normalization but the **global nonorthogonal boundary completion** that should turn the renormalized self-energy sum into a positive residual on the Critical-half range.
``````````

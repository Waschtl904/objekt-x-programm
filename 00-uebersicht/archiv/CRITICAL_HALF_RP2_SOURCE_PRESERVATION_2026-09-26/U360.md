# U360 — historische Originalfassung

**NOT_CANONICAL / HISTORICAL_SOURCE / NO_MATHEMATICAL_PROMOTION**

Diese Datei erhält die Originalbytes eines historischen Stands. Damalige Aussagen, Statuswörter und Prüfberichte werden als Quelle dokumentiert; sie erhalten hierdurch keine neue Geltung. Die fachliche Begutachtung wird in diesem Erhaltungspass nicht durchgeführt. Historischer Code, Workflow- und Statusdateien sind ausschließlich als Text eingebettet und werden nicht aktiviert.

- Versions-ID: `U360`.
- Quellcommit: `c866a977ae500d845ac869cfc878de2132381f6a`.
- Originalpfad: `audits/P11_STORAGE_GATE_CURRENT_2026-09-15.md`.
- Originalblob: `aadad0fa44e302c2e3d042ee3363da5175e2e08e`.
- SHA-256 der Originalbytes: `ddc97c59366292bfb6a367a7d6401dc71852a5902ec92ee96b968fea9bacd00e`.
- Umfang: 5199 Bytes; UTF-8, LF; abschließendes LF im Original: ja.
- Zusätzliche Trennbytes vor dem Schluss-Fence: 0. Diese gehören nicht zum Original; maßgeblich sind die ersten 5199 Bytes nach dem Eröffnungs-Fence.
- [Original im Commit](https://github.com/Waschtl904/objekt-x-programm/blob/c866a977ae500d845ac869cfc878de2132381f6a/audits/P11_STORAGE_GATE_CURRENT_2026-09-15.md) · [Familienindex](README.md).

Der folgende Textblock enthält unveränderte Originalbytes. Relative Quellverweise behalten ihren damaligen Pfad-/Commitkontext; sie werden hier nicht als aktive Links oder Abhängigkeiten ausgeführt.

``````````text
# P11 / Object X — Current relative-storage gate

**Datum:** 15. September 2026

## 0. Current verdict

The hard audit has reduced the branch to one nontrivial structural question. Local prime and archimedean pieces are no longer mysterious:

```text
Prime p-power tower     = discrete Critical-half OU precision
Gamma ground            = continuous Critical-half OU precision
P11 Hub+Rest            = covariance/state metric of the same discrete OU state
Riemann-R background    = positive continuum mixture of the same Euler cells
higher Gamma layer      = positive stable first-order filter bank
```

All of these statements are exact. None closes RH.

The unresolved issue is exclusively the **relative global coupling** between discrete prime spacings `h=log p` and the smooth Riemann-R continuum `rho(h)dh`.

---

## 1. Canonical local cell

For `h>0`, `q=e^{-h/2}`, define

```math
B_h
=\sqrt{\frac{h q(1+q)}{1-q}}
(T_h-I)(I-qT_h)^{-1}.
```

Then

```math
B_h^*B_h
=\sum_{k\ge1}h e^{-kh/2}K_{kh}^*K_{kh}.
```

At `h=log p` this is the complete positive p-power Weil tower. The same `q` is the P11 AR(1) transition.

The cell has a passive KYP realization, but the centered Weil contribution is not positive cellwise.

---

## 2. P11 covariance versus Weil precision

For the sampled field

```math
X_{p,k}=\sqrt h\,e^{-kh/4}K_{kh}v
```

and P11 tail state

```math
Y_{p,m}=\sqrt{1-q^2}\sum_{k\ge m}q^{k-m}X_{p,k},
```

Hub+Rest is the covariance/state form

```math
\sum_m\|Y_{p,m}\|^2
+\frac{q^2}{1-q^2}\|Y_{p,1}\|^2,
```

while the positive p-power Weil tower is the precision form

```math
\frac1{1-q^2}\sum_m\|Y_{p,m}-qY_{p,m+1}\|^2.
```

Thus the earlier search for a contraction between P11 and COMMON-JUMP was the wrong category. The canonical bridge is inverse-covariance metric duality.

---

## 3. Continuous limit

With

```math
X_v(t)=e^{-t/4}K_tv,
\qquad
Y(u)=\int_u^\infty e^{-(t-u)/2}X_v(t)dt,
```

one has

```math
(1/2-\partial_u)Y=X_v
```

and

```math
E_{Gamma,0}(v)
=\int\|X_v(t)\|^2dt
=\int\|Y'(u)-Y(u)/2\|^2du.
```

So Gamma ground is literally the continuum precision limit of the prime/P11 geometry.

---

## 4. Smooth reference uses the same cells

The positive density

```math
\rho(h)=\frac{d}{dh}R(e^h)
=\sum_{m\ge1}\frac{h^{m-1}}{m!\zeta(m+1)}
```

satisfies

```math
\int_0^\infty\rho(h)
\sum_{k\ge1}h e^{-kh/2}\delta_{kh}(dt)dh
=(e^{t/2}-e^{-t/2})dt.
```

Its continuous Euler product is

```math
Z_0(S)=\exp\int_0^\infty\rho(h)
\log(1-e^{-Sh})^{-1}dh
=\frac{S}{S-1}.
```

After `S=1/2+s`, the inverse is the lossless Critical-half allpass

```math
\frac{s-1/2}{s+1/2}.
```

The state realization

```math
\dot x=-x/2+u,
\qquad y=u-x
```

satisfies

```math
\frac{y}{u}=\frac{s-1/2}{s+1/2},
\qquad
\frac{d}{dt}|x|^2=|u|^2-|y|^2.
```

Hence the smooth reference itself has a forward positive/lossless realization.

---

## 5. Prime-only exact form

For the centered cell response

```math
C_v(h)=\|B_hv\|^2
-2\frac{h e^{-h/2}}{1-e^{-h/2}}\|v\|^2,
```

NULLPOL gives

```math
Q_W(v)
=\sum_p C_v(\log p)
-\frac12\int_0^\infty\rho(h)C_v(h)dh
+E_{Gamma,>=1}(v)
-(\kappa_*-4)\|v\|^2.
```

All prime powers are compressed into one cell per prime. The transform is invertible by ordinary Moebius inversion, so no autocorrelation data have been discarded.

---

## 6. What is already excluded

```text
orthogonal direct sum of local passive cells          insufficient
ordinary passive cascade of normalized Euler cells    collapses to zero
root-only cross-prime correction                       insufficient
cellwise centered positivity                           false
pointwise multiplier positivity                        false
static Gamma frame repair                              false
```

The passive cascade collapse is elementary: for fixed real `s>0`,

```math
\prod_{p\le P}
\frac{1-p^{-1/2}}{1-p^{-(s+1/2)}}\to0.
```

Thus a nontrivial infinite-prime transfer necessarily requires a **relative discrete/continuum renormalization**.

---

## 7. Literature firewall

The scale operator

```math
(Ag)(h)=h\sum_{k\ge1}g(kh)
```

has Mellin symbol `zeta(s+1)`. This is in the classical Muentz/Nyman-Beurling family; no novelty is claimed for that Mellin-zeta mechanism.

Similarly, Euler products in the critical strip and their relation to RH have a substantial literature. The project must not claim novelty for analytic continuation of Euler products or for positive-real formulations of `xi'/xi`.

---

## 8. Single remaining gate

A substantive Object-X mechanism now requires a **non-diagonal relative storage kernel** coupling

```text
discrete scale cells h=log p
          to
continuous cells rho(h) dh
          plus
higher Gamma reservoir,
```

with all local metrics already fixed by the OU/KYP constructions.

The coupling must be derived from the existing overlap-cone/stopped-OU incidence; no coefficient may be fitted from Weil positivity or from the zero set.

A PASS would produce a forward positive storage realization of the known Lagarias transfer. A FAIL for the natural overlap-cone kernel class should close this branch as an Object-X architecture while preserving the exact covariance/precision identities as independent mathematics.
``````````

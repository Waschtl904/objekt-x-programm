# U361 — historische Originalfassung

**NOT_CANONICAL / HISTORICAL_SOURCE / NO_MATHEMATICAL_PROMOTION**

Diese Datei erhält die Originalbytes eines historischen Stands. Damalige Aussagen, Statuswörter und Prüfberichte werden als Quelle dokumentiert; sie erhalten hierdurch keine neue Geltung. Die fachliche Begutachtung wird in diesem Erhaltungspass nicht durchgeführt. Historischer Code, Workflow- und Statusdateien sind ausschließlich als Text eingebettet und werden nicht aktiviert.

- Versions-ID: `U361`.
- Quellcommit: `68c09bb1b7e8d12f2113b4394612e465883d7fd4`.
- Originalpfad: `audits/P11_TOTAL_BOUNDARY_WEYL_SELF_ENERGY_NORMAL_FORM_2026-09-15.md`.
- Originalblob: `b1147253431cd6af58946ff92bb30e4ec451072f`.
- SHA-256 der Originalbytes: `ee1c3ea87ff3f04cbc2b916759698b8b69bcd406c880f5db30be4ae6ea67a89e`.
- Umfang: 13959 Bytes; UTF-8, LF; abschließendes LF im Original: ja.
- Zusätzliche Trennbytes vor dem Schluss-Fence: 0. Diese gehören nicht zum Original; maßgeblich sind die ersten 13959 Bytes nach dem Eröffnungs-Fence.
- [Original im Commit](https://github.com/Waschtl904/objekt-x-programm/blob/68c09bb1b7e8d12f2113b4394612e465883d7fd4/audits/P11_TOTAL_BOUNDARY_WEYL_SELF_ENERGY_NORMAL_FORM_2026-09-15.md) · [Familienindex](README.md).

Der folgende Textblock enthält unveränderte Originalbytes. Relative Quellverweise behalten ihren damaligen Pfad-/Commitkontext; sie werden hier nicht als aktive Links oder Abhängigkeiten ausgeführt.

``````````text
# P11 Audit — total boundary Weyl self-energy normal form

**Datum:** 15. September 2026  
**Basis:** Critical-half/NULLPOL, COMMON-JUMP diagonal finite part, RP2 Line-Weyl, Prime-Circle-Weyl, eingefroren bei `04e2256919d2726c31db7febe7810a137a8ff77d`.  
**Rolle:** theorem-level gemeinsame Prime/Archimedes-Normalform und exakte Reduktion des fixed-window NP-GAP auf einen komprimierten Boundary-Weyl-Gate.  
**Registry:** unveraendert.  
**Nonclaim:** kein positiver Gesamtblock, kein NP-GAP, kein Object-X-Abschluss, kein RH-Beweis, keine Publikationsneuheit.

---

## 0. Kurzurteil

Die Prime-Zylinder und der `RP^2`-Bulk besitzen nun denselben longitudinalen Boundary-Typ. Ihre renormalisierten Self-energies koennen deshalb zu **einer einzigen skalaren Boundary-Weyl-Funktion** addiert werden.

Fuer ein festes Quellfenster `[-a,a]` setze

```math
\boxed{
\Omega_a(z)
:=
2\pi\Sigma_\infty(z)
+
\sum_{p\le e^{2a}}
(\log p)W_p(z\log p),
}
```

mit

```math
W_p(\theta)
:=P_{p^{-1/2}}^{\mathbb D}(\theta)-1
```

und der regularisierten `RP^2`-Line-Weyl-Funktion `Sigma_infty`, fuer die

```math
\Phi_\infty(z)-\kappa_*
=-2\pi\Sigma_\infty(z).
```

Dann gilt exakt nach einer gleichzeitig auf Energie und Threshold angewandten Exterior-Gauge-Vervollstaendigung:

```math
\boxed{
\widetilde{\mathcal E}_a(z)
=\Omega_a(0)-\Omega_a(z),
}
```

```math
\boxed{
\widetilde\Gamma_a
=\Omega_a(0),
}
```

und daher

```math
\boxed{
\widetilde{\mathcal E}_a(z)-\widetilde\Gamma_a
=-\Omega_a(z).
}
```

Auf der NULLPOL-Klasse folgt somit

```math
\boxed{
Q_W(v)
=-\langle v,\Omega_a(D)v\rangle,
\qquad
v\in\mathscr D_{NP,a}.
}
```

Da bereits

```math
\mathscr D_{NP,a}
=L_{1/2}C_c^\infty(-a,a),
\qquad
L_{1/2}=D^2+1/4,
```

bewiesen ist, lautet der fixed-window Positivitaetsgate jetzt exakt:

```math
\boxed{
\langle L_{1/2}u,
\Omega_a(D)L_{1/2}u\rangle
\le0
\quad
\forall u\in C_c^\infty(-a,a).
}
```

Die offene Frage ist damit eine **komprimierte Gesamt-Weyl-Negativitaet auf der Critical-half-Range**, nicht mehr eine typunscharfe Prime-/Archimedes-Verklebung.

Status:

```text
prime and Gamma common boundary type                    ✓[M]
exact total self-energy Omega_a                         ✓[M]
exterior gauge completion preserves centered form       ✓[M]
completed positive backbone = Omega_a(0)-Omega_a(D)     ✓[M]
completed threshold = Omega_a(0)                        ✓[M]
centered Weil = -Omega_a(D) on NULLPOL                  ✓[M]
NULLPOL = Ran L_1/2 on compact windows                  ✓[M]
NP-GAP = compressed total-Weyl negativity               ✓[M] equivalence
positive boundary-triple completion                     ?[O]
P11 Feshbach supplies the required noncircular completion ?[O]
Object X / RH                                            ?[O]
```

---

# 1. Prime Self-energy recap

For every prime `p` define

```math
\ell_p=\log p,
\qquad
q_p=p^{-1/2}.
```

The Critical-half massive Green function on the twisted prime circle gives

```math
G_p(\theta)
=P_{q_p}^{\mathbb D}(\theta)
=1+2\sum_{k\ge1}q_p^k\cos(k\theta).
```

Remove the zero-winding universal-cover term:

```math
\boxed{
W_p(\theta)
:=G_p(\theta)-1
=2\sum_{k\ge1}q_p^k\cos(k\theta).
}
```

Then

```math
\boxed{
W_p(0)=2\sum_{k\ge1}q_p^k.
}
```

The gauge-completed positive prime tower has Fourier symbol

```math
\boxed{
(\log p)[W_p(0)-W_p(z\ell_p)].
}
```

Its simultaneously completed diagonal mass is

```math
\boxed{(\log p)W_p(0).}
```

Therefore the centered prime channel is

```math
\boxed{-(\log p)W_p(z\ell_p).}
```

---

# 2. Archimedean Self-energy recap

Let

```math
Y=RP^2,
\qquad
P=\sqrt{\Delta_Y+1/4}.
```

The regularized point-/line-Weyl function is denoted

```math
\Sigma_\infty(z).
```

The RP2 audits prove

```math
\boxed{
\kappa_*=2\pi\Sigma_\infty(0),
}
```

and

```math
\boxed{
\Phi_\infty(z)
=2\pi[
\Sigma_\infty(0)-\Sigma_\infty(z)
].
}
```

Thus the centered Gamma layer is

```math
\boxed{
\Phi_\infty(z)-\kappa_*
=-2\pi\Sigma_\infty(z).
}
```

This is exactly the same algebraic pattern as the completed Prime channel.

---

# 3. Why only finitely many primes enter `Omega_a`

If

```math
\log p>2a,
```

then for every `k>=1`

```math
k\log p>2a.
```

For `v` supported in `[-a,a]`, the two translated copies in `K_{k log p}v` are disjoint and therefore

```math
\|K_{k\log p}v\|^2
=2\|v\|^2.
```

Hence every centered contribution from that prime is exactly zero.

It is therefore sufficient and canonical at fixed `a` to sum

```math
p\le e^{2a}.
```

This is a finite prime set.

---

# 4. Exterior Gauge completion prime by prime

The original finite-window COMMON-JUMP positive prime energy contains only powers with

```math
k\ell_p\le2a.
```

For every omitted `k` with

```math
k\ell_p>2a
```

add simultaneously

```math
(\log p)q_p^k\|K_{k\ell_p}v\|^2
```

to the positive energy and

```math
2(\log p)q_p^k\|v\|^2
```

to the diagonal threshold.

Since

```math
\|K_{k\ell_p}v\|^2=2\|v\|^2,
```

the centered form changes by exactly zero.

Thus the completed positive energy and completed threshold differ from the original finite bookkeeping, but

```math
\boxed{
\widetilde{\mathcal E}_{p,a}
-\widetilde\Gamma_p I
=
\mathcal E_{p,a}^{finite}
-\Gamma_{p,a}^{finite}I
}
```

on the source window.

This is exactly the existing cutoff-gauge covariance, applied to the full geometric tail of an already active prime.

---

# 5. Total Self-energy

Define

```math
\boxed{
\Omega_a(z)
=
2\pi\Sigma_\infty(z)
+
\sum_{p\le e^{2a}}
(\log p)W_p(z\ell_p).
}
```

The sum is finite and every summand is a real even function for real `z`.

At zero frequency

```math
\boxed{
\Omega_a(0)
=
\kappa_*
+
\sum_{p\le e^{2a}}
(\log p)W_p(0).
}
```

This is the **gauge-completed diagonal mass**.

It need not equal the original finite `Gamma_a` term by term because all exterior powers of already active primes have been added to both sides. Their difference is an explicit positive scalar gauge amount.

---

# 6. Positive Backbone = zero-frequency drop of `Omega_a`

Add the archimedean positive backbone and all completed prime backbones.

Its symbol is

```math
\begin{aligned}
\widetilde{\mathcal E}_a(z)
&=
2\pi[\Sigma_\infty(0)-\Sigma_\infty(z)]\\
&\quad+
\sum_{p\le e^{2a}}
(\log p)
[W_p(0)-W_p(z\ell_p)].
\end{aligned}
```

Therefore

```math
\boxed{
\widetilde{\mathcal E}_a(z)
=\Omega_a(0)-\Omega_a(z).
}
```

Since it was built from sums of squared COMMON-JUMP features and the positive Gamma ladder,

```math
\boxed{
\Omega_a(0)I-\Omega_a(D)
\succeq0
}
```

on the full longitudinal `L^2` form domain.

This positivity is **unconditional** and is not the NP-GAP statement.

---

# 7. Completed Threshold = `Omega_a(0)`

The completed prime diagonal mass is

```math
\sum_{p\le e^{2a}}
(\log p)W_p(0).
```

The archimedean finite-part mass is

```math
\kappa_*=2\pi\Sigma_\infty(0).
```

Hence

```math
\boxed{
\widetilde\Gamma_a
=\Omega_a(0).
}
```

Consequently

```math
\boxed{
[\Omega_a(0)I-\Omega_a(D)]
-\Omega_a(0)I
=-\Omega_a(D).
}
```

This is the exact completed centered form.

---

# 8. Recovery of the original centered Weil form

The project COMMON-JUMP identity gives on NULLPOL

```math
Q_W(v)
=\mathcal E_a^{finite}(v)
-\Gamma_a^{finite}\|v\|^2.
```

By the primewise exterior gauge completion of §4, the same difference is

```math
\widetilde{\mathcal E}_a(v)
-\widetilde\Gamma_a\|v\|^2.
```

Using §§6--7 yields

```math
\boxed{
Q_W(v)
=-\langle v,\Omega_a(D)v\rangle.
}
```

Equivalently in Fourier variables,

```math
\boxed{
Q_W(v)
=-\int_{\mathbb R}
|\widehat v(z)|^2
\Omega_a(z)\,dz.
}
```

No new positivity assumption is used in this derivation.

---

# 9. NULLPOL turns this into a Critical-half range problem

The Critical-half range theorem gives

```math
\boxed{
\mathscr D_{NP,a}
=L_{1/2}C_c^\infty(-a,a),
\qquad
L_{1/2}=D^2+1/4.
}
```

Therefore write

```math
v=L_{1/2}u.
```

Then

```math
\boxed{
Q_W(L_{1/2}u)
=-\langle L_{1/2}u,
\Omega_a(D)L_{1/2}u\rangle.
}
```

Since `L_{1/2}` and `Omega_a(D)` are Fourier multipliers on the full line, they commute on the common test domain:

```math
Q_W(L_{1/2}u)
=-\langle u,
L_{1/2}^2\Omega_a(D)u\rangle.
```

The compact-support restriction remains essential; one must not replace

```math
L_{1/2}C_c^\infty(-a,a)
```

by the unrestricted full-line range of `L_{1/2}`.

---

# 10. Exact reformulation of fixed-window NP-GAP

The desired fixed-window positivity statement is

```math
Q_W(v)\ge0
\qquad
(v\in\mathscr D_{NP,a}).
```

By §8 this is equivalent to

```math
\boxed{
\langle v,\Omega_a(D)v\rangle\le0
\qquad
(v\in\mathscr D_{NP,a}).
}
```

Using the range theorem, equivalently

```math
\boxed{
\langle L_{1/2}u,
\Omega_a(D)L_{1/2}u\rangle\le0
\quad
(u\in C_c^\infty(-a,a)).
}
```

Thus the NP-GAP is exactly a **compressed total Boundary-Weyl negativity problem**.

The multiplier `Omega_a(z)` is not pointwise nonpositive; the compact-support/NULLPOL compression is therefore essential.

---

# 11. Common positive Bulk origin

Every summand of `Omega_a` has already been realized as a renormalized line-defect Boundary response of a positive Critical-half bulk.

### Prime `p`

```math
\mathcal L_p
=(D_x+D_s)^2+1/4
```

on

```math
\mathbb R_x\times S^1_{\log p},
```

with line defect `R_x x {0}` and renormalized response

```math
W_p(D_x\log p).
```

### Archimedes

```math
\mathcal L_\infty
=D_x^2+\Delta_{RP^2}+1/4
```

on

```math
\mathbb R_x\times RP^2,
```

with line defect `R_x x {y_0}` and regularized response

```math
\Sigma_\infty(D_x).
```

Hence `Omega_a(D_x)` is algebraically the weighted sum of line-defect responses of the direct-sum positive bulk family

```math
\boxed{
\mathscr H_a^{bulk}
=
L^2(\mathbb R\times RP^2)
\oplus
\bigoplus_{p\le e^{2a}}
L^2(\mathbb R\times S^1_{\log p}).
}
```

The natural weights are `2pi` for the `RP^2` response and `log p` for the prime-circle responses.

### Boundary-triple firewall

This section identifies the direct-sum **fiberwise Weyl response**. A complete unbounded boundary-triple construction with all distributional line traces and the `RP^2` finite part in one closed operator system is not yet claimed.

---

# 12. Canonical indefinite Feshbach picture — and why it is not enough

At a formal boundary-response level, a positive bulk `L` coupled to a boundary variable through `J` produces a Schur term

```math
-J^*L^{-1}J.
```

The identity

```math
Q_W=-\Omega_a(D)
```

therefore has the **sign of a bulk self-energy Schur complement with zero boundary diagonal**.

But the corresponding block

```math
\begin{pmatrix}
0&J^*\\
J&L
\end{pmatrix}
```

is in general indefinite. Thus this observation is not a positivity proof.

To turn the bulk picture into a positive Object-X dilation one needs an independently defined boundary block `A_a` such that the full block is positive while the required elimination/renormalization still returns the centered Weil form.

Choosing `A_a` only by demanding that positivity hold would be circular.

---

# 13. Relation to the existing P11 Feshbach term

P11 already supplies an independent positive boundary operator

```math
\Sigma_R^{P11}
=H_R(I+R_R^*R_R)^{-1}H_R^*\succeq0.
```

This operator is forward-defined from source-conditioned Hub/Rest data and is not obtained from Weil positivity.

However the first-threshold audit proves that

```text
P11 Feshbach term != centered COMMON-JUMP term
```

term by term.

The new total-Weyl normal form sharpens the remaining problem:

> Can the forward P11 boundary geometry provide the missing noncircular **boundary completion / quotient / finite-part mechanism** for the direct-sum Critical-half bulk whose renormalized Weyl response is `Omega_a(D)`?

This is a precise operator question. It is not solved here.

---

# 14. Why this is stronger than the previous harmonic normal form

The earlier Poisson audit gave

```text
Prime   -> disk Poisson kernels,
Gamma   -> half-plane Poisson kernels.
```

The current normal form adds three pieces of structure:

1. every Prime Poisson kernel is the point Green/Weyl response of a concrete Critical-half prime circle;
2. the Gamma layer is the point/line Weyl response of a concrete `RP^2` Critical-half bulk;
3. all responses act on the **same longitudinal boundary Hilbert space** and sum to one total self-energy `Omega_a`.

Thus the bridge is now operator-typed rather than merely harmonic-analytic.

---

# 15. New exact search target

The next admissible Object-X candidate must address the operator

```math
\boxed{\Omega_a(D)}
```

and the compact-support Critical-half range

```math
\boxed{L_{1/2}C_c^\infty(-a,a).}
```

A successful forward construction would need a radius-compatible family of bulk/boundary spaces and connecting maps for which:

```text
1. the direct-sum Prime-circle/RP2 Weyl response is Omega_a;
2. the P11 source-conditioned nonorthogonal boundary geometry is present before shorting;
3. exterior Prime powers are absorbed as gauge under R<S;
4. the RP2 finite part is canonical and compatible with the same transport;
5. the resulting positive Gram/Schur object has Q_W as the required residual form,
   without assuming Q_W>=0 to define the missing block.
```

Any proposal failing one of these tests is not a forward Object-X construction.

---

# 16. Forschungsurteil

The finite-window problem has reached the exact form

```math
\boxed{
Q_W
=-P_{NP,a}\,\Omega_a(D)\,P_{NP,a}
}
```

at the quadratic-form level, where `P_{NP,a}` denotes restriction/compression to the compact-window NULLPOL class, not an unrestricted full-line orthogonal projector.

The total `Omega_a` is no longer an unexplained multiplier: it is the sum of explicit renormalized line-defect Self-energies of positive Critical-half bulks.

What remains open is **precisely** the sign of this total Weyl response after Critical-half range compression and the existence of a noncircular positive boundary completion supplied by the already existing P11/Feshbach geometry.

No stronger claim is made.
``````````

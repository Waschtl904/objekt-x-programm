# U010 — historische Originalfassung

**NOT_CANONICAL / HISTORICAL_SOURCE / NO_MATHEMATICAL_PROMOTION**

Diese Datei erhält die Originalbytes eines historischen Stands. Damalige Aussagen, Statuswörter und Prüfberichte werden als Quelle dokumentiert; sie erhalten hierdurch keine neue Geltung. Die fachliche Begutachtung wird in diesem Erhaltungspass nicht durchgeführt. Historischer Code, Workflow- und Statusdateien sind ausschließlich als Text eingebettet und werden nicht aktiviert.

- Versions-ID: `U010`.
- Quellcommit: `6b5956e69dfcb8124e8e31083613fd89c5f35fb3`.
- Originalpfad: `.github/workflows/np-prolate-1-arb.yml`.
- Originalblob: `e86ad5ac83f87ae2db719ac133b50b0a50c51fb7`.
- SHA-256 der Originalbytes: `cc5fba009279bd587cf3bb95da16082b9dcc1dfbaf8ebc69ab317f69f139fd1a`.
- Umfang: 1488 Bytes; UTF-8, LF; abschließendes LF im Original: ja.
- Zusätzliche Trennbytes vor dem Schluss-Fence: 0. Diese gehören nicht zum Original; maßgeblich sind die ersten 1488 Bytes nach dem Eröffnungs-Fence.
- [Original im Commit](https://github.com/Waschtl904/objekt-x-programm/blob/6b5956e69dfcb8124e8e31083613fd89c5f35fb3/.github/workflows/np-prolate-1-arb.yml) · [Familienindex](README.md).

Der folgende Textblock enthält unveränderte Originalbytes. Relative Quellverweise behalten ihren damaligen Pfad-/Commitkontext; sie werden hier nicht als aktive Links oder Abhängigkeiten ausgeführt.

``````````text
name: NP-PROLATE-1 Arb certificate
on:
  pull_request:
  workflow_dispatch:
permissions:
  contents: read
jobs:
  np-prolate-1:
    strategy:
      fail-fast: false
      matrix:
        sector: [even, odd]
    runs-on: ubuntu-latest
    timeout-minutes: 60
    steps:
      - uses: actions/checkout@11bd71901bbe5b1630ceea73d27597364c9af683 # v4.2.2
        with:
          ref: ${{ github.event.pull_request.head.sha || github.sha }}
          persist-credentials: false
      - uses: actions/setup-python@a26af69be951a213d495a4c3e4e4022e16d87065
        with:
          python-version: '3.13'
      - name: Install exact numerical dependency
        run: python -m pip install python-flint==0.9.0
      - name: Run NP-PROLATE-1 exact-head certificate
        run: |
          set -euo pipefail
          git rev-parse HEAD
          git hash-object scripts/check_np_prolate_1_arb.py
          python --version
          python scripts/check_np_prolate_1_arb.py "${{ matrix.sector }}" | tee "np_prolate_1_${{ matrix.sector }}.log"
          grep -q "NP-PROLATE-1 ${{ matrix.sector == 'even' && 'EVEN' || 'ODD' }} PASS ZERTIFIZIERT" "np_prolate_1_${{ matrix.sector }}.log"
          grep -q "ALL REQUESTED NP-PROLATE-1 SECTORS PASS ZERTIFIZIERT" "np_prolate_1_${{ matrix.sector }}.log"
      - uses: actions/upload-artifact@ea165f8d65b6e75b540449e92b4886f43607fa02 # v4
        with:
          name: np-prolate-1-${{ matrix.sector }}
          path: np_prolate_1_${{ matrix.sector }}.log
``````````

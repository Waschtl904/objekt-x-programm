# U190 — historische Originalfassung

**NOT_CANONICAL / HISTORICAL_SOURCE / NO_MATHEMATICAL_PROMOTION**

Diese Datei erhält die Originalbytes eines historischen Stands. Damalige Aussagen, Statuswörter und Prüfberichte werden als Quelle dokumentiert; sie erhalten hierdurch keine neue Geltung. Die fachliche Begutachtung wird in diesem Erhaltungspass nicht durchgeführt. Historischer Code, Workflow- und Statusdateien sind ausschließlich als Text eingebettet und werden nicht aktiviert.

- Versions-ID: `U190`.
- Quellcommit: `69bfe2e585a9fa5ca87a85c5ec98faa8a20e856d`.
- Originalpfad: `audits/P11_PRIMEWISE_INNOVATION_SHORTING_NOGO_2026-09-15.md`.
- Originalblob: `7c5aae9a6088fc8372fc91838fbc7ff20309d21c`.
- SHA-256 der Originalbytes: `d2ae54c84cecf6e597527c492dcc87347631daaac91ddbf09b5bc707b9fb74e0`.
- Umfang: 7056 Bytes; UTF-8, LF; abschließendes LF im Original: ja.
- Zusätzliche Trennbytes vor dem Schluss-Fence: 0. Diese gehören nicht zum Original; maßgeblich sind die ersten 7056 Bytes nach dem Eröffnungs-Fence.
- [Original im Commit](https://github.com/Waschtl904/objekt-x-programm/blob/69bfe2e585a9fa5ca87a85c5ec98faa8a20e856d/audits/P11_PRIMEWISE_INNOVATION_SHORTING_NOGO_2026-09-15.md) · [Familienindex](README.md).

Der folgende Textblock enthält unveränderte Originalbytes. Relative Quellverweise behalten ihren damaligen Pfad-/Commitkontext; sie werden hier nicht als aktive Links oder Abhängigkeiten ausgeführt.

``````````text
# P11 Audit — Primewise innovation-shorting no-go on the physical jump range

**Datum:** 15. September 2026  
**Branch:** `research/critical-half-green-tree-bridge-2026-09-13`  
**Rolle:** enger Klassen-No-Go fuer den direkten P11 -> COMMON-JUMP Abstieg.  
**Registry:** unveraendert.  
**Nonclaim:** kein No-Go gegen gekoppelte Root-/Gamma-/Fenster-/Maskenkonstruktionen.

---

## 0. Kurzurteil

Der naheliegende Versuch, die diagonale COMMON-JUMP-Primegeometrie direkt als kontraktives Shorting der P11-AR(1)-Innovationsgeometrie zu erhalten, scheitert bereits auf **einem einzelnen Primast** und sogar nach Einschraenkung auf die tatsaechlich von Translationen erzeugten Kanalsequenzen.

Fuer

```math
q=p^{-1/2}\in(0,1)
```

sei

```math
(T_qX)_m
=\sqrt{1-q^2}\sum_{k\ge m}q^{k-m}X_k,
```

so dass

```math
T_q^*T_q=R_q-uu^*,
\qquad
R_q(j,k)=q^{|j-k|},
\qquad
u_k=q^k.
```

Die COMMON-JUMP-Kanaele eines einzelnen Fouriermodus mit Phase

```math
\theta=z\log p
```

haben, nach Herausziehen des gemeinsamen Faktors `sqrt(log p)`, die Form

```math
\boxed{
X_k(\theta)
=q^{k/2}\,2\sin\frac{k\theta}{2}.
}
```

Definiere

```math
\mathcal R_q(\theta)
:=
\frac{\|T_qX(\theta)\|_{\ell^2}^2}
{\|X(\theta)\|_{\ell^2}^2}
```

fuer `theta` mit `X(theta) != 0`.

Dann gilt fuer jedes `0<q<1`

```math
\boxed{
\lim_{\theta\to0}\mathcal R_q(\theta)>1,
\qquad
\mathcal R_q(\pi)<1.
}
```

Damit expandiert `T_q` einige physikalische Jump-Richtungen und kontrahiert andere. Folglich existiert weder in der Richtung

```text
COMMON-JUMP bare channel -> P11 innovation
```

noch in der Rueckrichtung ein primeweiser kontraktiver Identifikator, der auf dem gesamten translationsgenerierten Kanalraum einfach `T_q` bzw. `T_q^{-1}` realisiert.

Status:

```text
physical translation-channel parametrization      ✓[M]
low-frequency expansion of T_q                    ✓[M]
pi-frequency contraction of T_q                   ✓[M]
primewise contraction/shorting route              ×[M]
coupled Root+Gamma+mask route                     ?[O]
```

---

# 1. Physical channel range

For the prime `p`, put

```math
h=\log p,
\qquad
q=e^{-h/2}=p^{-1/2}.
```

The common-jump channel at prime power `p^k` is

```math
K_{kh}=T_{kh/2}-T_{-kh/2}.
```

On a Fourier mode of frequency `z`, its scalar multiplier is

```math
2i\sin\frac{kzh}{2}.
```

The Weil weight is

```math
w_{p,k}=(\log p)q^k.
```

After discarding the harmless common factors `sqrt(log p)` and `i`, the weighted channel sequence is therefore exactly

```math
\boxed{
X_k(\theta)=2q^{k/2}\sin(k\theta/2),
\qquad
\theta=zh.
}
```

Thus this no-go is not tested on arbitrary `ell^2`: it is tested on the actual one-parameter physical channel curve generated by the translation representation.

---

# 2. Low-frequency limit

As `theta -> 0`,

```math
X_k(\theta)
=k\theta q^{k/2}+O(\theta^3k^3q^{k/2}).
```

Hence the limiting ratio is the ratio for

```math
x_k=kq^{k/2}.
```

Write

```math
r=\sqrt q\in(0,1),
\qquad
a=qr=r^3.
```

For `x_k=kr^k`, the tail transform is

```math
\begin{aligned}
(T_qx)_m
&=\sqrt{1-q^2}\sum_{k\ge m}q^{k-m}kr^k\\
&=\sqrt{1-q^2}\,r^m
\left(
\frac{m}{1-r^3}
+
\frac{r^3}{(1-r^3)^2}
\right).
\end{aligned}
```

Using

```math
\sum_{m\ge1}q^m=\frac q{1-q},
\quad
\sum_{m\ge1}mq^m=\frac q{(1-q)^2},
\quad
\sum_{m\ge1}m^2q^m=\frac{q(1+q)}{(1-q)^3},
```

one obtains after simplification

```math
\boxed{
\lim_{\theta\to0}\mathcal R_q(\theta)
=
\frac{(1+r)
\left(r^8+2r^7+4r^6+6r^5+8r^4+6r^3+4r^2+2r+1\right)}
{(1-r)(r^2+r+1)^4}.
}
```

Subtracting `1` gives the manifestly positive expression

```math
\boxed{
\lim_{\theta\to0}\mathcal R_q(\theta)-1
=
\frac{r^3
\left(
2r^6+6r^5+12r^4+16r^3+17r^2+11r+4
\right)}
{(1-r)(r^2+r+1)^4}
>0.
}
```

Thus the P11 innovation map strictly **expands** sufficiently low-frequency physical jump directions.

---

# 3. The phase `theta=pi`

At `theta=pi`,

```math
X_{2m}=0,
\qquad
X_{2m+1}=2(-1)^m q^{m+1/2}.
```

The bare diagonal norm is

```math
\boxed{
\|X(\pi)\|^2
=\frac{4q}{1-q^2}.
}
```

Since

```math
T_q^*T_q=R_q-uu^*,
```

the AR(1) part is

```math
\langle X,R_qX\rangle
=
\frac{4q(1-q^3)}{(1-q^2)(1+q^3)},
```

while the root component is

```math
|\langle u,X\rangle|^2
=
\frac{4q^3}{(1+q^3)^2}.
```

Therefore

```math
\boxed{
\mathcal R_q(\pi)
=
\frac{(1-q)(1+q^4)}
{(1+q)(1-q+q^2)^2}.
}
```

Moreover

```math
\boxed{
1-\mathcal R_q(\pi)
=
\frac{q^2(2q^3-2q^2+q+1)}
{(1+q)(1-q+q^2)^2}
>0.
}
```

Indeed

```math
2q^3-2q^2+q+1
=1+q-2q^2(1-q)>0
```

for `0<q<1` (and positivity is immediate also from the displayed polynomial on the interval).

Thus `T_q` strictly **contracts** the physical channel at `theta=pi`.

---

# 4. Contractive feature-shorting consequences

Suppose one tries to realize the bare COMMON-JUMP channel geometry from the P11 innovation target by a primewise contraction `C_p` satisfying

```math
C_p(T_qX(\theta))=X(\theta)
```

for every physical phase `theta`.

Contractivity would require

```math
\|X(\theta)\|\le\|T_qX(\theta)\|
```

for every `theta`, i.e.

```math
\mathcal R_q(\theta)\ge1.
```

This fails at `theta=pi`.

Conversely, a primewise contraction carrying bare channels to P11 innovations,

```math
C_pX(\theta)=T_qX(\theta),
```

would require

```math
\mathcal R_q(\theta)\le1
```

for every `theta`, which fails for sufficiently small nonzero `theta`.

Therefore both naive primewise contractive directions are excluded.

---

# 5. Scope firewall

This no-go excludes only the class in which:

1. each prime branch is treated independently;
2. the P11 root has already been separated/conditioned out;
3. no archimedean/continuous compensator is coupled to the branch;
4. no spatial P11 masks `P_{Omega_{p,k-1,R}}` are used;
5. the intertwiner is required to be a contraction between the bare COMMON-JUMP channel norm and the P11 innovation norm on the full physical translation-channel range.

It does **not** exclude:

```text
- a joint root + innovation construction;
- cancellation against the continuous compensator e^{t/2}dt;
- a spatially masked/radius-dependent Schur complement;
- coupling between prime branches through the common root;
- coupling to the higher Gamma channel h_1;
- a larger conservative/unitary colligation whose compression is not primewise.
```

---

# 6. Strategic consequence

The new prime-discrepancy audit shows that after NULLPOL the natural arithmetic object is

```math
Delta=nu_P-e^{t/2}dt.
```

The present no-go shows simultaneously that the **innovation branch alone** cannot be identified contractively with the COMMON-JUMP diagonal geometry.

Thus any surviving Object-X architecture based on P11 must use at least one of the pieces that the naive branchwise reduction discards:

```math
\boxed{
\text{common root / continuous compensator / spatial masks / Gamma coupling}.
}
```

This sharply narrows the next gate: test whether the common P11 root plus the canonical continuous compensator can be assembled into a forward positive parent whose Schur complement produces the full discrepancy pairing, not merely its scalar root trace.
``````````

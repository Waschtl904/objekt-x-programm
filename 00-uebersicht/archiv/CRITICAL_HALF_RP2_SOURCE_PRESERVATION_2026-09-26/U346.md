# U346 — historische Originalfassung

**NOT_CANONICAL / HISTORICAL_SOURCE / NO_MATHEMATICAL_PROMOTION**

Diese Datei erhält die Originalbytes eines historischen Stands. Damalige Aussagen, Statuswörter und Prüfberichte werden als Quelle dokumentiert; sie erhalten hierdurch keine neue Geltung. Die fachliche Begutachtung wird in diesem Erhaltungspass nicht durchgeführt. Historischer Code, Workflow- und Statusdateien sind ausschließlich als Text eingebettet und werden nicht aktiviert.

- Versions-ID: `U346`.
- Quellcommit: `6ef15df81881d7f8bd86195b376cca50300860dc`.
- Originalpfad: `audits/P11_ROOT_MASS_MONOTONE_TRANSPORT_2026-09-16.md`.
- Originalblob: `6c06687b2ed14d4e6261054ef4b045fb3ee1e49a`.
- SHA-256 der Originalbytes: `6aa77b437b25ca7819a0ad52d34c893576ae70634237e79be11cc79f96ef45b1`.
- Umfang: 7104 Bytes; UTF-8, LF; abschließendes LF im Original: ja.
- Zusätzliche Trennbytes vor dem Schluss-Fence: 0. Diese gehören nicht zum Original; maßgeblich sind die ersten 7104 Bytes nach dem Eröffnungs-Fence.
- [Original im Commit](https://github.com/Waschtl904/objekt-x-programm/blob/6ef15df81881d7f8bd86195b376cca50300860dc/audits/P11_ROOT_MASS_MONOTONE_TRANSPORT_2026-09-16.md) · [Familienindex](README.md).

Der folgende Textblock enthält unveränderte Originalbytes. Relative Quellverweise behalten ihren damaligen Pfad-/Commitkontext; sie werden hier nicht als aktive Links oder Abhängigkeiten ausgeführt.

``````````text
# P11 Audit — Monotone root-mass transport to the smooth storage line

**Datum:** 16. September 2026  
**Branch:** `research/critical-half-green-tree-bridge-2026-09-13`  
**Registry:** unveraendert.  
**Status:** exact construction plus imported classical PNT estimate `✓[K/M]`; no RH/Object-X closure.

---

## 0. Result

Let `n_1<n_2<...` run through all prime powers and put

```math
h_j=\log n_j,
\qquad
a_j=\frac{\Lambda(n_j)}{n_j}>0.
```

These are exactly the base-spacing weighted **squared P11 root amplitudes**:

```math
a_j=(\log p)\,|\langle\Phi_o,\Phi_{p,k}\rangle|^2
\qquad(n_j=p^k).
```

Define cumulative mass coordinates

```math
s_0=\gamma,
\qquad
s_j=\gamma+\sum_{i\le j}a_i,
```

and intervals

```math
I_0=[0,\gamma),
\qquad
I_j=[s_{j-1},s_j)\quad(j\ge1).
```

Then

```math
|I_j|=a_j.
```

The classical prime number theorem with de la Vallee-Poussin error implies

```math
\boxed{
s_j-h_j
=O\!\left(\sqrt{h_j}\,e^{-c\sqrt{h_j}}\right),
}
```

for some `c>0`; in particular

```math
\boxed{
\sup_{L\in I_j}|L-h_j|\longrightarrow0.
}
```

Thus the arithmetic root measure plus the Euler-constant boundary mass admits a canonical order-preserving equal-mass embedding into the smooth root-storage line. No zeros and no RH are used.

Let

```math
\mathscr H_P^{root}
=\mathbb C_{\gamma}\oplus\ell^2(a_j),
```

with norm

```math
\|(c,x)\|^2
=\gamma|c|^2+\sum_ja_j|x_j|^2.
```

Define

```math
(U(c,x))(L)
=c\,1_{I_0}(L)
+\sum_jx_j1_{I_j}(L).
```

Then

```math
\boxed{
U:\mathscr H_P^{root}\to L^2(\mathbb R_+,dL)
\text{ is an isometry.}
}
```

Its range is the closed subspace of functions constant on each arithmetic mass cell. The orthogonal complement is a canonical positive continuum reservoir.

Moreover, if the arithmetic delay semigroup is

```math
(D_P(s)x)_j=e^{-s h_j}x_j
```

and the continuum delay is

```math
(M_s f)(L)=e^{-sL}f(L),
\qquad \Re s\ge0,
```

then for every fixed `s` with `Re s>=0`,

```math
\boxed{
K_s:=M_sU-UD_P(s)
}
```

is compact. Its nonzero singular values (up to the finite boundary coordinate) are exactly

```math
\sigma_j(s)^2
=\frac1{a_j}\int_{I_j}
|e^{-sL}-e^{-s h_j}|^2dL,
```

and `sigma_j(s)->0` by the localization above.

This gives the first explicit global **relative** Hilbert coupling on the branch for which the discrete prime-power delay geometry and the smooth root line differ only by a compact operator. It does not imply positivity of the Weil form; the Critical-half rescaling and higher physical channel structure still have to be attached.

---

# 1. PNT input for the weighted cumulative root mass

Let

```math
\psi(x)=\sum_{n\le x}\Lambda(n)=x+E(x).
```

Import the classical unconditional de la Vallee-Poussin estimate

```math
E(x)=O\left(xe^{-c\sqrt{\log x}}\right).
```

Partial summation gives

```math
A(x):=\sum_{n\le x}\frac{\Lambda(n)}n
=\frac{\psi(x)}x+
\int_1^x\frac{\psi(t)}{t^2}dt.
```

Hence

```math
A(x)
=\log x+1+rac{E(x)}x
+\int_1^x\frac{E(t)}{t^2}dt.
```

The Laurent expansion of `-zeta'/zeta` at `1` gives the limiting constant

```math
A(x)-\log x\longrightarrow-\gamma.
```

Equivalently,

```math
\boxed{
A(x)
=\log x-\gamma
+\frac{E(x)}x
-\int_x^\infty\frac{E(t)}{t^2}dt.
}
```

The imported error estimate yields

```math
\frac{E(x)}x
=O(e^{-c\sqrt{\log x}}),
```

and with `u=sqrt(log t)`,

```math
\int_x^\infty e^{-c\sqrt{\log t}}\frac{dt}{t}
=2\int_{\sqrt{\log x}}^\infty ue^{-cu}du
=O\left((1+\sqrt{\log x})e^{-c\sqrt{\log x}}\right).
```

Therefore

```math
\boxed{
\gamma+A(x)
=\log x
+O\left((1+\sqrt{\log x})e^{-c\sqrt{\log x}}\right).
}
```

At `x=n_j`, this is precisely the claimed estimate for `s_j-h_j`.

The estimate is `✓[K/M]`: the reduction is elementary, but it imports the classical unconditional PNT error theorem.

---

# 2. Entire arithmetic interval lies near its physical depth

Since

```math
s_{j-1}=s_j-a_j,
```

and

```math
a_j=\frac{\Lambda(n_j)}{n_j}\to0,
```

one has

```math
\sup_{L\in I_j}|L-h_j|
\le |s_j-h_j|+a_j\to0.
```

Thus the equal-mass transport does not merely match cumulative endpoints: each whole arithmetic cell collapses onto its original logarithmic depth asymptotically.

---

# 3. Isometric root-storage embedding

By construction the intervals are pairwise disjoint and

```math
|I_0|=\gamma,
\qquad |I_j|=a_j.
```

Therefore

```math
\begin{aligned}
\|U(c,x)\|_{L^2(dL)}^2
&=\gamma|c|^2+\sum_j a_j|x_j|^2\\
&=\|(c,x)\|_{\mathscr H_P^{root}}^2.
\end{aligned}
```

Since

```math
\sum_ja_j=\infty
```

by `A(x)~log x`, the intervals exhaust the whole half-line. The range of `U` is the piecewise-constant arithmetic-cell subspace, not all of `L^2(dL)`. Its orthogonal complement consists of zero-mean fluctuations inside the mass cells and supplies a canonical positive reservoir rather than being discarded.

---

# 4. Relative delay mismatch is compact

For the normalized atomic basis vector supported on cell `j`, the mismatch has squared norm

```math
\sigma_j(s)^2
=\frac1{a_j}\int_{I_j}
|e^{-sL}-e^{-s h_j}|^2dL.
```

For `Re s>=0`, the map `L -> e^{-sL}` is Lipschitz on `R_+` with constant at most `|s|`. Hence

```math
\sigma_j(s)
\le |s|\sup_{L\in I_j}|L-h_j|
\longrightarrow0.
```

The mismatch images of distinct normalized atomic basis vectors have disjoint supports, hence are orthogonal. Therefore `K_s` is a diagonal-singular-value operator with singular values `sigma_j(s)` plus a finite-rank boundary contribution. Since `sigma_j(s)->0`,

```math
\boxed{K_s\text{ is compact}.}
```

The statement is pointwise in `s`; no `H^infty`-norm compactness is claimed.

---

# 5. Relation to the critical finite-part transfer

The cumulative endpoint defect is exactly

```math
s(L)-L
=\gamma+M_P(L)-L
=-e^{-L/2}J_\Delta(L)
```

away from atom conventions, where

```math
M_P(L)=\sum_{\log n<L}\frac{\Lambda(n)}n.
```

Thus the monotone transport displacement is precisely the unscaled Critical-half finite-part defect already found in the Volterra audit.

The new construction therefore gives that scalar discrepancy a concrete geometric role:

```text
J_Delta = Critical-half rescaling of the displacement between
          arithmetic root-mass coordinates and smooth storage depth.
```

This is not a positivity theorem. It is a forward relative Hilbert embedding derived from the same stopped P11 root masses.

---

# 6. Remaining firewall

Several facts remain open:

1. Compact relative delay mismatch does not imply positive-realness of the assembled zeta transfer.
2. The critical multiplication by `e^{L/2}` that converts root-square jumps `Lambda(n)/n` into Weil jumps `Lambda(n)/sqrt(n)` is unbounded on the plain root-storage line.
3. The full physical P11/COMMON-JUMP channel has higher-rank tail directions, not only the scalar root coordinate.
4. The construction must still reproduce the exact `R=1` Prime-2 mixed witness before it can count as an Object-X candidate.

The next gate is therefore to determine whether the Critical-half source operator `D_+=partial+1/2` supplies exactly the graph norm needed to make the rescaled transport bounded, while retaining the compact relative delay geometry above.
``````````

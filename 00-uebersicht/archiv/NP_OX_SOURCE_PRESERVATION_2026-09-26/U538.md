# Historische Originalfassung U538

Status: **NOT_CANONICAL / HISTORICAL_PROVENANCE / CONTENT_PRESERVED**. Mathematische Begutachtung bleibt offen beziehungsweise behält ihren früher dokumentierten Stand. Diese Einbettung aktiviert weder historische Programme noch frühere Forschungsfronten.

- Originalpfad: `scripts/check_np_prolate_1_arb.py`
- Quellcommit: `4b65625ef651ce60945d045869b0cfaa7cffd3f3`
- Originalblob: `0163f33d79c6879da4712a1251b7bf2671ffc55f`
- SHA-256: `6193757c5a63d2062a2bb70e96dc0dab106bf751386287afddcb5c60c6767ede`
- Originallänge: 1581 Bytes; abschließendes LF: true.

Der eingerückte Status gehört zur Archivhülle; alle Aussagen im folgenden Original bleiben historisch. Falls dem Original das letzte LF fehlt, liegt das eine Trenn-LF vor dem Schlusszaun außerhalb der angegebenen Originallänge.

``````````text
# -*- coding: utf-8 -*-
"""Certificate-repair wrapper for the frozen PR #116 Arb checker.

The theorem/checker snapshot at 6b5956e... is preserved byte-for-byte in
check_np_prolate_1_arb_base.py. This wrapper changes only ROOT_EPS, enlarging
all pre-booked root uncertainty intervals. Every enlarged bracket is still
charged by the original root_edge_trace_upper proof path.
"""

import hashlib
import sys
from decimal import Decimal
from pathlib import Path

import check_np_prolate_1_arb_base as base

EXPECTED_BASE_BLOB = "b73ab9858cbc19626fb19794ab4ce4a0a7d3dbd8"
BASE_PATH = Path(__file__).with_name("check_np_prolate_1_arb_base.py")
raw = BASE_PATH.read_bytes()
blob = hashlib.sha1(b"blob " + str(len(raw)).encode("ascii") + b"\0" + raw).hexdigest()
if blob != EXPECTED_BASE_BLOB:
    raise RuntimeError(f"base checker blob mismatch: {blob} != {EXPECTED_BASE_BLOB}")

# Frozen checker used 1e-7. The first exact CI run showed both diagnostic root
# centers were slightly outside that radius. We enlarge conservatively by 100x.
# The original checker books every entire root bracket into the trace error.
base.ROOT_EPS = Decimal("0.00001")

print(f"BASE CHECKER BLOB VERIFIED: {blob}")
print(f"CERT REPAIR ROOT_EPS={base.ROOT_EPS}")

which = sys.argv[1].lower() if len(sys.argv) > 1 else "all"
if which not in ("even", "odd", "all"):
    raise SystemExit("usage: check_np_prolate_1_arb.py [even|odd|all]")
sectors = ["even", "odd"] if which == "all" else [which]
for sector in sectors:
    base.run_sector(sector)
print("ALL REQUESTED NP-PROLATE-1 SECTORS PASS ZERTIFIZIERT")
``````````

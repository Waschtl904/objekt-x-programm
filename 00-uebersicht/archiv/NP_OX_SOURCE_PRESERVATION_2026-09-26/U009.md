# Historische Originalfassung U009

Status: **NOT_CANONICAL / HISTORICAL_PROVENANCE / CONTENT_PRESERVED**. Mathematische Begutachtung bleibt offen beziehungsweise behält ihren früher dokumentierten Stand. Diese Einbettung aktiviert weder historische Programme noch frühere Forschungsfronten.

- Originalpfad: `.github/workflows/np-prolate-1-arb.yml`
- Quellcommit: `5721efb401c4b8128d6be7513c559bc3f86f1581`
- Originalblob: `5e33783d4b15e37ee26d0266eb6d1b85579a2769`
- SHA-256: `55dc46bb68fc81674b3e6dd33919d567e3c699ecf71566d1805b7f96f4de6fae`
- Originallänge: 1513 Bytes; abschließendes LF: true.

Der eingerückte Status gehört zur Archivhülle; alle Aussagen im folgenden Original bleiben historisch. Falls dem Original das letzte LF fehlt, liegt das eine Trenn-LF vor dem Schlusszaun außerhalb der angegebenen Originallänge.

``````````text
name: NP-PROLATE-1 Arb certificate
on:
  pull_request:
  workflow_dispatch:
permissions:
  contents: read
jobs:
  np-prolate-1:
    strategy:
      fail-fast: false
      matrix:
        sector: [even, odd]
    runs-on: ubuntu-latest
    timeout-minutes: 180
    steps:
      - uses: actions/checkout@11bd71901bbe5b1630ceea73d27597364c9af683 # v4.2.2
        with:
          ref: ${{ github.event.pull_request.head.sha || github.sha }}
          persist-credentials: false
      - uses: actions/setup-python@a26af69be951a213d495a4c3e4e4022e16d87065
        with:
          python-version: '3.13'
      - name: Install exact numerical dependency
        run: python -m pip install python-flint==0.9.0
      - name: Run NP-PROLATE-1 exact-head certificate
        run: |
          set -euo pipefail
          git rev-parse HEAD
          git hash-object scripts/check_np_prolate_1_arb.py
          python --version
          python -u scripts/check_np_prolate_1_arb.py "${{ matrix.sector }}" | tee "np_prolate_1_${{ matrix.sector }}.log"
          grep -q "NP-PROLATE-1 ${{ matrix.sector == 'even' && 'EVEN' || 'ODD' }} PASS ZERTIFIZIERT" "np_prolate_1_${{ matrix.sector }}.log"
          grep -q "ALL REQUESTED NP-PROLATE-1 SECTORS PASS ZERTIFIZIERT" "np_prolate_1_${{ matrix.sector }}.log"
      - uses: actions/upload-artifact@ea165f8d65b6e75b540449e92b4886f43607fa02 # v4
        if: always()
        with:
          name: np-prolate-1-${{ matrix.sector }}
          path: np_prolate_1_${{ matrix.sector }}.log
``````````

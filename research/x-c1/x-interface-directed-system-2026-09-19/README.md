# Local source directed system and Objekt-X interface

**AUTHOR-DERIVED / EXTERNAL-REVIEW-OPEN**, 2026-09-19.

This package separates two levels which had previously been easy to conflate.

**C0 is already canonical on the current Shell-Schur band.** Physical
zero-extension gives a compositional directed system of closed two-Mellin form
spaces. The Hermitian Weil form, parity, Prime correlations and the
archimedean jump pairing are natural under these embeddings.

**C1 remains open.** Objekt X requires intrinsic positive local readouts and
compatible mediator embeddings which intertwine with the C0 source maps and
produce the Weil form as a Gram form without first assuming its positivity.

A shifted graph inner product `q+17 I` gives a genuine Hilbert direct-limit
carrier, but this is deliberately not called Objekt X: its positivity comes
from the artificial shift.

The current transport gate is unchanged:
`MOVING 191D LOW-BLOCK + PROFILE RESERVE RENEWAL`.

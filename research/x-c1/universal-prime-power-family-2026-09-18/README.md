# Universal prime-power family

2026-09-18 — **AUTHOR-DERIVED / EXTERNAL-REVIEW-OPEN**.

This package closes the structural gate behind the connected interval program.

It proves on one fixed reference space:

- the exact unitary scaling from `H1_0(-a,a)`;
- the full derivation
  `q_a = D_H + V + q0(a) I - K_a - sum_q Lambda(q)/sqrt(q) T_{log(q)/a}`;
- the correct Prime-Power weight, including `w_4=log(2)/2`;
- bounded self-adjoint partial shifts for every `0<d<2`;
- the cutoff firewall `T_2=0` but `||T_d||=1` for `1<=d<2`;
- exact Mellin and form functoriality under zero extension;
- finiteness of every active prime-power set;
- the uniform full-tail Parseval interface supplied by the parent active-set theorem.

No new positive endpoint beyond `a=log(2)` is claimed. Future endpoints such as `log(5)/2` are instances of this architecture, not new operator constructions.

# Block-adaptive all-parity transport

**AUTHOR-DERIVED / EXTERNAL-REVIEW-OPEN**, 2026-09-19.
Anchor: `ca3849ab2a5abf6268ee892109a293332c1030e9`.

For B=log(5)/2 and exactly the original two Mellin conditions, every
nonzero actual complex source has physical gap >4*10^-15 through
B+10^-13, and >10^-15 through B+5*10^-13. The odd gap is >10^-12 on
both ranges. The outer width is one million times the anchor's width.

The full low block is measured in its own Schur energy metric. Its
shell coupling has an analytically integrated logarithmic/constant
model with a certified full-operator remainder. A near/far INPUT split
improves the complete tail/profile bound. All infinite tails, shell
profiles and mixed blocks remain included; no source is newly selected.

From the repository root, with Python standard library only:

```text
python -B research/x-c1/block-adaptive-profile-transport-2026-09-19/check_adaptive.py --verify
```

The calculation rebuilds both cores and all four 31-pivot comparisons,
passes 71 new exact checks,
replays the entire inherited chain, checks 103 input bindings, and
verifies byte-identical JSON/log plus seven payload hashes. It can take
several minutes. `PROOF.md` states the analytic arguments and the exact
scope; `STATUS_DE.md` gives the German research status.

This certifies a larger actual finite window at fixed core B. Adaptive
reserve renewal at moving endpoints and non-summable transport remain
open, as do reaching channel 7, Connected Unit-Window Coercivity,
Strong Terminal, Object X and global claims. Reproduction is not an
external audit. Both concurrent restart results are preserved.

Section 9 of the proof also corrects a ceiling inequality in concurrent
`6cbef9d`: its qualitative scalar-gap summability barrier remains valid
with a floor-based dyadic bound. Historical files are not overwritten.
